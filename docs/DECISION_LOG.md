# Decision Log

**Append-only.** One row per non-obvious choice, with its reason.

Rules:

1. **Never edit or delete a past row.** If a decision changes, add a new row that supersedes it and
   mark the old one `superseded by D-0NN`. The record of what we believed and when is the point.
2. Every row needs a **reason**, not just a choice. "Because it's better" is not a reason.
3. If a decision invalidates existing results, say which ones in the row.
4. Add the row **when you make the choice**, not when you remember to. This file writes half the
   method document; that only works if it is written contemporaneously.

Owner tags: **S** filter core · **M** learning · **D** data/eval · **P** maps · **A** Android · **C** submission.

---

## Seeded from the plan of record (26 Aug 2026)

These predate the repo. They are recorded here so the method write-up has a source, and so that
anyone who wants to revisit one can see what it was trading against.

| ID | Date | Decision | Reason | Owner |
|---|---|---|---|---|
| D-001 | 2026-08-26 | One continuously-running filter; GNSS is an optional gated correction. No second system, no handoff. | "Seamless transition within milliseconds" becomes true by construction. Two-system designs show a visible position jump on every tunnel entry. | S |
| D-002 | 2026-08-26 | Invariant EKF with state on SE₂(3) + biases, rather than a naive EKF. | Group-affine dynamics make the error linearisation state-independent: consistent covariance (no false yaw observability) and a large convergence basin from a badly wrong initial attitude — exactly the arbitrary-mount case. | S |
| D-003 | 2026-08-26 | Forward speed comes from a learned head. Acceleration is **never** double-integrated. | 10 mg of accel bias is ≈176 m of error in 60 s — 176% of the budget from one term. See [ERROR_BUDGET.md](ERROR_BUDGET.md) §2. | M |
| D-004 | 2026-08-26 | NHC + ZUPT + ZARU as always-on gated pseudo-measurements. | Cars do not slide sideways or fly; stopped cars have zero velocity and zero angular rate. ZARU is the primary means of observing gyro bias, the dominant error term. | S |
| D-005 | 2026-08-26 | Learned adaptive R_NHC (AI-IMU mechanism, ~6k params) rather than a fixed R. | The constraint must relax during hard cornering and tighten on straights. This is the mechanism behind the strongest published car result (1.10% on KITTI) and it is cheap. | M |
| D-006 | 2026-08-26 | Phone→vehicle rotation R_sv lives **in the filter state**; PCA on horizontal acceleration is an initialiser only. | One-shot alignment is fragile and cannot track a mount that shifts. Under NHC, R_sv is observable through turns and accelerations. Budget requires ~1° yaw accuracy ([ERROR_BUDGET.md](ERROR_BUDGET.md) §5). | S |
| D-007 | 2026-08-26 | IO-VNBD `S-` smartphone channels only. `V-` wheel-speed columns banned, enforced by a CI test. | Wheel odometry is disallowed by PS 26168. The columns sit in the same dataset and are trivially easy to leak in by accident. | D |
| D-008 | 2026-08-26 | Metrics are CTE (Cumulative True Error) and CRSE (Cumulative Root Square Error) per Onyekpe's definitions, **plus** drift as % of distance. | The task brief mislabels CTE/CRSE as cross-track error / ATE. Drift % is what PS 26168 actually grades, and no paper in this line reports it — we must compute it ourselves. | D |
| D-009 | 2026-08-26 | The evaluation harness is frozen before any model trains. | A protocol that can move will be moved, one justified step at a time, until the numbers flatter us and mean nothing. | D |
| D-010 | 2026-08-26 | Gate 1 (physics-only InEKF within 3–5× of the GNSS-available baseline) is a **hard stop** before any network is added. | Stacking learned components on a broken spine produces a system nobody can debug and numbers nobody can defend. | S |
| D-011 | 2026-08-26 | One C++/Rust core, two frontends: JNI to Android, and the identical library as the 200 Hz FOG edge engine. | Satisfies the "edge deployable engine" requirement by construction instead of as a second project. Avoids two divergent implementations of the filter. | S |
| D-012 | 2026-08-26 | Export FP16. INT8 only behind an explicit covariance-head degradation test. | A mis-scaled covariance corrupts a Kalman filter worse than a slightly noisy mean. Quantisation error on the variance output is not visible in ordinary accuracy metrics. | M |
| D-013 | 2026-08-26 | Android: `*_UNCALIBRATED` sensor types, foreground service, real timestamps, `HIGH_SAMPLING_RATE_SENSORS`. | Calibrated types silently subtract an OS bias estimate that the filter is also estimating, and the two fight. Since Android 9 there are no background sensor events; since API 31 the rate caps at 200 Hz without the permission. | A |
| D-014 | 2026-08-26 | NavIC is a bonus, not a dependency. Rely on GPS + GLONASS + Galileo + BeiDou. | Hardware support exists on several chipsets, but 2026 work finds NavIC signals often filtered out by low-level drivers, and Android's GNSS API lacks standardised NavIC identifiers. | A |
| D-015 | 2026-08-26 | Map matching is an online HMM over an offline OSM CSR graph, with emission σ driven by the filter's own covariance. | Feeding a filtered track into a matcher tuned for raw-GPS noise wastes the covariance we already compute. Sliding-window Viterbi commits segments older than ~5–10 s. | P |
| D-016 | 2026-08-26 | Car parks are a **separate mode**: tight NHC + barometer floor detection + ramp detection. Limitation stated honestly in the submission. | No road graph exists underground, so map matching is unavailable. This is the least certain claim we make and it is better said by us than found by a judge. | P |
| D-017 | 2026-08-26 | No pedestrian-network **weights** are reused (RoNIN, IONet, RIDI, TLIO, IDOL, CTIN). Architectures and filter frameworks only. | They encode a 0.5–2 m/s gait prior. A car at 16.7 m/s is off-distribution and they under-predict badly. | M |
| D-018 | 2026-08-26 | Every plot and table is stamped with the commit SHA and seed that produced it. | A result whose provenance is unknown cannot be defended or reproduced, and will be deleted rather than debugged. | D |

---

## Sprint 0 onward

| ID | Date | Decision | Reason | Owner |
|---|---|---|---|---|
| D-019 | 2026-08-26 | Repo root is the existing project folder; `AGENTS.md` and the two source documents stay at the root beside the scaffold. | Keeps the plan of record and its sources adjacent to the code rather than one directory up from it. | S |
| D-020 | 2026-08-26 | Default branch is `main`. | Convention; CI templates assume it. | S |
| D-021 | 2026-08-26 | **Deadline is Tue 8 Sep 2026, not Sat 20 Sep.** Sprint plan compressed from 25 days to 13. | Confirmed real portal cut-off. The AGENTS.md plan put Sprints 2 and 3 entirely *after* the actual deadline; 20 Sep is also a Sunday, not a Saturday. Supersedes the calendar in AGENTS.md — that file's sprint table is now stale and [SPRINT_BOARD.md](SPRINT_BOARD.md) is authoritative. | C |
| D-022 | 2026-08-26 | Screening filter is the **Python reference** in `core/reference/`; the C++/Rust port moves to post-screening (Oct). | D-011 (one core, two frontends) is unchanged as an architecture decision — only its timing moves. Writing the InEKF in C++/Rust before Gate 1 inside 13 days is the schedule risk most likely to sink the submission. Screening is graded on drift %, plots and method, not implementation language, and the edge-engine argument rests on the architecture rather than the language. | S |
| D-023 | 2026-08-26 | CRSE defaults to `SUM_SQUARES` = √(Σeᵢ²) rather than RMS, behind a `CrseConvention` enum. | Our definition source is a secondary summary. Consistency check against WhONet's 180 s baseline (CTE 2.90 m, CRSE 13.67 m, ~180 epochs) favours SUM_SQUARES: it implies ~1.0 m typical per-epoch error, where RMS would imply 13.67 m per epoch against a 2.90 m signed sum. **Reasoned, not verified** — seat D pins it against the paper's equations before Gate 0. | D |
| D-024 | 2026-08-26 | Torch is an optional extra; the harness and its CI run without it. | The harness is the critical path. Blocking Gate 0 on a 2 GB download, on a teammate's machine or in CI, trades days for nothing. | D |
| D-025 | 2026-08-26 | Unimplemented filter methods raise `NotImplementedError` rather than returning zeros. | A stub returning `None`/zeros lets the harness produce plausible all-zero trajectories and report them as results. Failing loudly is the only safe placeholder in a numerical pipeline. | S |
| D-026 | 2026-08-26 | NHC gating thresholds set conservatively: min speed 1 m/s, max yaw rate 0.6 rad/s, max lateral accel 4 m/s². | A missed NHC application costs a little accuracy; a false one during a slip injects a confident wrong measurement the filter believes. Asymmetric cost, so bias toward not applying. Revisit at Gate 1 against measured dynamics. | S |
| D-027 | 2026-08-26 | Predicted speed variance is floored at 1e-4 and the head regresses **log**-variance. | An unclamped variance head can output ~0 and hand the filter an infinitely-trusted measurement, driving the covariance singular in one step. Log-space keeps the output unconstrained and the loss well-conditioned. | M |

| D-028 | 2026-08-26 | The filter carries the **right-invariant** error `η = X̂X⁻¹`. GNSS, which is a left-invariant observation, is applied through the adjoint rather than the covariance being carried in its convention. | The always-on measurement family — NHC, ZUPT, ZARU, learned speed — is body-frame, which pairs with the right-invariant error; GNSS is optional and χ²-gated (D-001) and absent for the 10–180 s windows we are graded on. Verified consequence: the body-frame velocity Jacobian has an **exactly zero attitude column**, so the filter cannot acquire the spurious yaw observability that makes a naive EKF inconsistent. Right-invariant attitude error is also world-frame, which is the convergence-basin claim in D-002. | S |
| D-029 | 2026-08-26 | Nominal propagation uses the exact Γ₀/Γ₁/Γ₂ closed form, not a first-order Euler step; and `A_RI` is evaluated at the step **midpoint**. | At 10 Hz and 0.6 rad/s the Γ₁ correction is ~3% of the velocity increment and is rotation-direction-dependent, so it does not average out over the 1800 steps of a 180 s outage — it is a systematic heading-correlated drift, the error class the budget has no room for. Midpoint evaluation of `A` measured ~1800× lower linearisation residual at Δt = 1 ms. Both cost a handful of flops. | S |
| D-030 | 2026-08-26 | Mount-error perturbation convention is `R̂_sv = Exp(ξ_sv) R_sv` (vehicle-frame, left multiplication). | Makes `ξ_sv,z` literally mount-yaw error in the vehicle frame, so its covariance diagonal is directly comparable to the ~1° requirement in [ERROR_BUDGET.md](ERROR_BUDGET.md) §5 without a change of basis. A right-multiplied convention would have needed a rotation applied every time the budget was checked, which is a step someone eventually skips. | S |
| D-031 | 2026-08-26 | Discrete process noise `Q_d` by Van Loan matrix-exponential, not the `Φ G Q_c Gᵀ Φᵀ Δt` approximation. | Measured 4.2% error at Δt = 0.1 s with our own `FilterConfig` noise values. It mis-allocates noise between the position and velocity blocks, and an under-sized Q makes the filter reject good measurements at the χ² gate — a failure that looks like a sensor problem, not a tuning problem. Cost is one 36×36 `expm` per step, affordable in the Python reference; the October C++ port must validate any cheaper closed form against it. | S |
| D-032 | 2026-08-26 | Small-angle cutoff for the Γ functions is **1e-3**, not the 1e-6 first written down. | The guard must sit where the closed form is still accurate, not where it divides by zero. Sweeping the exact identities `Γ₀ = I + φ^∧Γ₁` / `Γ₁ = I + φ^∧Γ₂` puts the worst residual at φ ≈ 1.2e-6 — inside the branch a 1e-6 cutoff sends to the closed form, where `φ²+2cosφ−2` keeps almost no significant figures. Found by writing the test, not by reading the formula. | S |
| D-033 | 2026-08-26 | The SE₂(3) derivation is checked in CI ([tests/test_se23_derivation.py](../tests/test_se23_derivation.py)) against numerical ground truth, before the filter that uses it exists. | A wrong matrix on paper becomes a filter that runs, produces smooth plausible trajectories, and is undebuggable — and Gate 1 is a hard stop with no retry. Three corrections (D-029 midpoint, D-031 magnitude, D-032 cutoff) came out of writing the checks rather than out of reading the algebra, which is the argument for doing it in this order. | S |

---

## Seat P — transcribed 27 Aug 2026 from the Delhi Road Graph study (26 Aug)

These four were decided on 26 Aug and cited by ID in the seat-P artifact, but never reached this
file. Transcribed here so the record is complete; seat P confirms the wording. The gap itself is
logged as R-5 in [IMPLEMENTATION_PLAN.md](IMPLEMENTATION_PLAN.md) §2B.

| ID | Date | Decision | Reason | Owner |
|---|---|---|---|---|
| D-034 | 2026-08-26 | The NCR extract merges **both** Geofabrik `northern-zone` and `central-zone` before clipping. | Verified by point-in-polygon against Geofabrik's published `.poly` boundaries: Delhi, Gurugram, Faridabad, Sonipat and Alwar are northern; Noida, Greater Noida, Ghaziabad and Meerut are central. The seam is the Yamuna, so a northern-only graph loses every eastern crossing — DND Flyway, Akshardham, Kalindi Kunj — and the whole Noida–Ghaziabad side. A matcher whose graph ends mid-bridge does not degrade gracefully; it snaps to the nearest thing still in the graph. | P |
| D-035 | 2026-08-26 | Candidate search uses the **Mahalanobis distance against the filter's own `P_pos`**, not a scalar radius. | The 99% radius implied by our error budget runs from 18 m at a 10 s outage to 359 m at 180 s — a 20× span across the outage lengths the protocol mandates. Set it for 180 s and every parallel service road and flyover deck within 360 m enters the candidate set at 10 s; set it for 10 s and the true road is absent at 180 s. The terms are also anisotropic — at 60 s along-track error is 1.5× the combined lateral, and the ellipse rotates with heading — so a scalar σ is a circle fitted to a rotating ellipse. `d_M² = δᵀ P_pos⁻¹ δ` costs one 2×2 solve per candidate and is simply the correct quantity. | P |
| D-036 | 2026-08-26 | **Build the HMM matcher over our own CSR graph; do not adopt a library.** | Every candidate library takes GPS accuracy as one scalar — Valhalla Meili precomputes `1/(2·σ_z²)` with `σ_z` defaulting to 4.07 m, and FMM's `gps_error`, GraphHopper's `measurement_error_sigma` and Barefoot's `sigma` are the same shape. That is the right model for raw GPS traces and the wrong one for a filtered pose whose covariance we compute. Adopting one means either discarding D-015/D-035 or patching someone else's core probability model. Viterbi over the CSR is ~200 lines, and the CSR is ours regardless, because its mmap behaviour is what makes matching affordable on Android. | P |
| D-037 | 2026-08-26 | `highway=service` **is** included in the drivable graph, with highway class carried in a 4-bit edge flag as an emission prior. | Measured cost is +21% (57,922 service ways against 280,884 drivable), not the assumed 2×, so the memory objection collapses. Excluding forces a car genuinely on a service lane — one runs beside every Delhi arterial — onto the arterial. Including keeps it reachable but makes it out-argue a trunk road to win. | P |

---

## Consolidation — 27 Aug 2026

Arising from [IMPLEMENTATION_PLAN.md](IMPLEMENTATION_PLAN.md), which supersedes the sprint calendars
in AGENTS.md and SPRINT_BOARD.md and the standalone *Master Implementation Plan — SIH26168 (ISRO)*
document in its entirety.

| ID | Date | Decision | Reason | Owner |
|---|---|---|---|---|
| D-038 | 2026-08-27 | **`Q_c` is seeded from IO-VNBD's own >20 min stationary segments, not from a team phone.** Our own phone's Allan run continues in parallel but is no longer Gate-1-blocking. | The graded numbers come from IO-VNBD, recorded on a Huawei P20 Pro / Moto G7 Power / BlackBerry Priv. Tuning process noise from a different device models a sensor that produces none of the evaluated data. 20 min at 10 Hz gives ARW cleanly at τ = 1 s and bias instability out to τ ≈ 100–200 s — the exact timescale of a 10–180 s outage. It also removes Android from the Gate 1 critical path. Supersedes the Sprint 0 seat-S and seat-A Allan items in [SPRINT_BOARD.md](SPRINT_BOARD.md). | S |
| D-039 | 2026-08-27 | The screening demo is a **static replay renderer over harness output**: no FastAPI, no React, no WebSocket, no map SDK, and **no simulator**. | A live cockpit fed by its own scenario generator can display numbers the evaluation never produced — the failure the frozen protocol exists to prevent. Constraining the demo to render `eval/figures/` output makes a screenshot self-evidencing and costs ~1.5 days instead of ~3. Supersedes the entire frontend and `scenarios/` sections of the Master Implementation Plan. | A |
| D-040 | 2026-08-27 | The adaptive-covariance network adapts **`R_NHC` (measurement noise)**, never `Q` (process noise). | Brossard's AI-IMU CNN outputs the measurement-noise covariance of the NHC pseudo-measurements — how tightly to enforce "no sideways motion" at each step. The Master Implementation Plan attributes dynamic `Q_k` adaptation to the same paper, which does not support it. Implementing `Q_k` would be an unvalidated method carried by a citation that does not back it. Reaffirms and sharpens D-005. | M |
| D-041 | 2026-08-27 | **MapmyIndia/Mappls is not a screening dependency.** The offline OSM CSR graph (D-034–D-037) is the committed map path; any Indian map SDK is a post-screening rendering layer only. | An online SDK behind an API key cannot back a "100% offline" claim. Measured footprint for the offline path is ~21 MiB (Delhi bbox) / ~32 MiB (core NCR) with ~16 KiB resident over a 180 s transit, so there is no capability argument for the dependency either. | P |
| D-042 | 2026-08-27 | **No performance figure enters the deck, write-up or demo unless `eval/run.py` produced it** under the frozen protocol, stamped with commit and seed. | The Master Implementation Plan carries "RMSE < 1.0 m", ">95% drift reduction" and "<2 ms edge inference" as targets. Unbacked headline numbers are the cheapest way to lose an adversarial question, and they migrate from plan to slide silently. Extends D-018 from figures to every quoted number. | C |
| D-043 | 2026-08-27 | `core/ffi/` ships as an **interface definition** at screening — a header/trait file, not an implementation. | D-022 defers the C++/Rust port to October, but PS 26168 asks for an edge-deployable engine and the argument rests on the architecture. A ~100-line interface that both the Android build and the 200 Hz FOG build bind to converts a promise into an artefact for half a day of work, without reopening D-022's schedule reasoning. | S |
| D-044 | 2026-08-27 | **The frozen split names sequences by bare stem (`S3a`, `Vta11`, `M`), and the 11 stems with no `S-` smartphone file are removed from it.** Long-outage drops 9 → 3 sequences, mandatory plots 4 → 2, train 7 → 5. `eval/splits.UNAVAILABLE_S_STREAM` records the excluded stems and a new CI test rejects any split that names one. | Two defects, found by listing the IO-VNBD file tree before downloading it. (1) The `V-` prefix denotes the *stream*, not the sequence — `load_split` globs `**/V-St6.csv`, matches the ECU file and the leakage guard rejects it, so the entire held-out set was unloadable and the failure read as a wheel-speed bug. (2) `St1`, `St6`, `St7`, `Y2`, `Vtb13`, `Vfb01c`, `Vfb02a`, `Vfb02b`, `Vfb02d`, `Vfb02e`, `Vfb02g` ship on the `V-` stream **only** — in neither the synchronised nor the unsynchronised folder is there an `S-` counterpart — so PS 26168 puts them out of reach. The protocol in [EVALUATION.md](EVALUATION.md) §3 was drafted from the paper, which is written against the vehicle stream. **Open:** seat D re-picks replacements from the stems that do have an `S-` file and updates §3 before the Gate 0 freeze; until then the long-outage result rests on three sequences and must be reported as such. | D |

---

## Sprint 0 — R-1, Allan variance from IO-VNBD (28 Aug 2026)

| ID | Date | Decision | Reason | Owner |
|---|---|---|---|---|
| D-045 | 2026-08-28 | **`Q_c` is seeded from the measured values `gyro_arw = 4.11e-4` rad/s/√Hz (1.41 °/√hr), `accel_vrw = 7.46e-3` m/s²/√Hz (0.45 m/s/√hr), with `gyro_bias_rw = 5.48e-5` rad/s²/√Hz and `accel_bias_rw = 1.04e-3` m/s³/√Hz *derived* rather than measured.** Measured bias instability: gyro 42 °/hr at τ ≈ 45 s, accel 0.34 mg at τ ≈ 20 s. Segments: `S-T2[31422:36490]` (507 s) and `S-T7[47809:52285]` (448 s), overlapping Allan deviation per axis at 10 Hz, worst axis taken. Tool: [`eval/allan.py`](../eval/allan.py); artefacts in `eval/figures/allan_*.csv`, stamped. Executes R-1 and closes R-2. | Four things, in order of how much they change the answer. (1) **The premise was wrong.** [DATASETS.md](DATASETS.md) §1.4 promised ">20 min" stationary segments; sweeping all 168 distinct `S-` files, the longest continuous, uniformly-sampled, still stretch is 507 s. τ_max is therefore ~51 s at the T/10 confidence limit, not the 100–200 s the plan assumed — so bias instability is an upper bound (the curve is still descending at τ_max on most axes) and the +½ rate-random-walk slope is unreachable. The bias driving noises come instead from a first-order Gauss–Markov model on the two measured quantities, `q = B√(2/τ_c)`, and are labelled as derived everywhere they appear. (2) **Stationary is not the same as quiet.** `S-A6`'s segments pass the ZUPT detector and carry 20–30× the mechanical energy of the `S-T` ones, returning an apparent ARW ~6× larger. That measures the cabin, not the gyroscope; segments must sit an order of magnitude inside the ZUPT thresholds to characterise a sensor, and in-cabin vibration becomes an explicit Q inflation term at Gate 1 instead. (3) **A coefficient is only an ARW if the band is white.** Five of six accelerometer fits failed a ±0.15 check on the −½ slope — a parked car really does accelerate at low frequency — so VRW rests on one axis (`accel_z`, S-T7) and says so rather than averaging three numbers of which two are not VRWs. (4) The result, 1.41 °/√hr, sits inside the 0.5–5 °/√hr phone range our own budget states, replacing a 3.0e-3 placeholder that was 10.3 °/√hr — 7× pessimistic and outside that range (R-2, self-flagged in [SE23_PROPAGATION.md](SE23_PROPAGATION.md) §10). | S |
| D-046 | 2026-08-28 | **Bias instability is `B = σ_min / 0.664`, a division.** [ERROR_BUDGET.md](ERROR_BUDGET.md) §9's earlier "the flat minimum gives bias instability (×0.664)" is corrected in place, and `eval.allan.BIAS_INSTABILITY_COEFF` is defined as `√(2 ln2 / π)` with a test pinning both the constant and the direction. | IEEE Std 952's flicker-floor relation is `σ(τ) = B √(2 ln2/π) = 0.664 B`, so the Allan minimum is 0.664 *of* B. Multiplying instead of dividing understates B by 2.27×, and it understates it in the dangerous direction: the filter would believe a drifting gyro bias more than it should, exactly where the error budget has least room (lateral error is 40 m of a 100 m budget and is gyro-bias-driven). The phrasing was ambiguous rather than plainly wrong, which is why it survived a review — hence the test rather than only a doc fix. | S |
| D-047 | 2026-08-28 | **The `S-` loader's column allowlist gains explicit aliases for the header spellings IO-VNBD actually ships, and reads the files as latin-1.** Guard semantics are unchanged: the aliases map shipped spellings onto names already on the allowlist and add no new permitted concept. | The allowlist was written against the paper's column names, not the CSVs, and no code in the repo had ever loaded the dataset — so this was invisible until R-1 needed real bytes. Every real `S-` file was rejected wholesale: `ACCELEROMETER X (m/s²)` normalises generically to `accelerometer_x`, which is not on the allowlist, and all three `ORIENTATION (Yaw\|Pitch\|Roll)` columns collapse to the single name `orientation` once parenthesised groups are stripped. The headers also carry raw 0xB0/0xB5 bytes, so the default utf-8 read raises before the guard runs at all. Two further spellings ship (`GYROSCOPE X/Y/Z` for `Yaw/Pitch/Roll` — verified byte-identical on `S-S1`, which appears in both folders, so this is AndroSensor labelling and not a different axis order; and `SATELLITES IN RANGE` without the `GPS` prefix). The `time since start` alias is anchored on its `(ms` unit specifically so the `V-` stream's `Time Since Start of Day (seconds)` cannot be laundered into an allowed name; a test asserts that it is not. A guard that rejects everything is not safer than one that works — it is untested, and it would have been "fixed" under deadline pressure by someone switching the allowlist off. | S |

<!-- Add new rows below. Do not edit rows above. -->


## Sprint 1 — P-02, `InEKF.propagate()` on SE₂(3) (29 Aug 2026)

| ID | Date | Decision | Reason | Owner |
|---|---|---|---|---|
| D-048 | 2026-08-29 | **The mount block of `Q_c` is zero.** `FilterConfig.mount_rw = 0.0`, so `σ_sv² I₃` contributes nothing, and `P` on the `ξ_sv` block neither grows nor shrinks under propagation. Pinned by `test_the_mount_block_of_q_carries_no_process_noise_yet`. | [SE23_PROPAGATION.md](SE23_PROPAGATION.md) §5.3 writes `Q_c = diag(σ_g², σ_a², σ_bg², σ_ba², σ_sv²)` "from `FilterConfig`", but `FilterConfig` has no `σ_sv` and **no source in this repo gives it a magnitude** — not the Allan run (D-045 characterises the IMU, not the mount), not the error budget, not the derivation. Under G-4 the choice is between inventing a number and stating the model actually in force, and a guessed value here is worse than an absent one: it lands inside the covariance that the χ² gate, the uncertainty ellipse and the map matcher's emission σ all read, where being wrong is invisible rather than loud. Zero says "the mount is modelled as rigid", which is exactly what this phase implements — nothing yet estimates `R_sv` or re-inflates it. P-11 owns the value together with the `detect_mount_disturbance` wiring that makes a non-zero one meaningful; until then the ~1° mount requirement in [ERROR_BUDGET.md](ERROR_BUDGET.md) §5 rests on the PCA initialiser alone, and that is a stated limitation rather than a modelled one. | S |
| D-049 | 2026-08-29 | **`A_RI` and `G_RI` are evaluated at the same step-midpoint state.** D-029 fixes the midpoint only for `A`; this extends it to `G`. | Van Loan's block matrix `[[−A, G Q_c Gᵀ], [0, Aᵀ]]` mixes the two inside a single matrix exponential, so they are not independent choices — evaluating them at different states linearises one factor of the same product at `t₀` and the other at `t₀+Δt/2`. `G_RI` carries the identical `−v̂^∧R̂` and `−p̂^∧R̂` blocks that motivated D-029 in the first place, and both move by the same amount over the step, so the argument for the midpoint applies unchanged. The derivation is silent because §8.2 names only `A`; recorded here rather than left as a silent constant, per the P-02 instruction to log every place the derivation was ambiguous enough to require a choice. | S |

---

## Sprint 1 — P-03, the update family (31 Aug 2026)

| ID | Date | Decision | Reason | Owner |
|---|---|---|---|---|
| D-050 | 2026-08-31 | **The measurement correction is *subtracted*, not added.** `X̂⁺ = Exp(−δ[0:9]) X̂`, `b̂⁺ = b̂ − δ`, `R̂_sv⁺ = Exp(−δ[15:18]) R̂_sv`. [SE23_PROPAGATION.md](SE23_PROPAGATION.md) §8.3 writes all four with a plus and is superseded on this point; the doc fix is outside P-03's file scope and is flagged for seat S. | §5 defines the right-invariant error as `η_R = X̂X⁻¹`, so `X̂ = Exp(ξ)X` and `ξ` is the error **in the estimate** — likewise `δb_g = b̂_g − b_g`. Every Jacobian in §7 is the Jacobian of that subsection's innovation with respect to that same `ξ`: GNSS `z = p̂ − p_gnss` gives `+I` on the position block, ZARU `z = ω̃ − b̂_g = −δb_g` gives `−I` on the gyro-bias block. So `δ = Kz` estimates the error, and removing an error means subtracting it. The sign is set by §5 alone and is **not** a free convention: writing the innovation the other way round flips `H` with it, `K` picks up the second sign, and `δ` is unchanged — which is why §8.3 is wrong rather than merely different. Verified independently by SE₂(3) test 10: the direct and adjoint GNSS paths agree to 1e-9 only with this sign. A flipped correction does not fail loudly; it produces a filter that diverges slowly and smoothly. | S |
| D-051 | 2026-08-31 | **ZUPT uses §7.1's body-frame Jacobian `H_vb = [0, R̂ᵀ, 0, 0, 0, 0]`, not §7.2's vehicle-frame `H_veh`.** §7.2's "ZUPT takes all three with `v_veh = 0`" is superseded on the frame; the assertion is unchanged. Pinned by `test_zupt_does_not_shrink_the_mount_covariance`. | The two forms assert the same thing — `R_sv` is a rotation, so `R_sv R̂ᵀv̂ = 0` exactly when `R̂ᵀv̂ = 0` — but §7.2 carries a `−v̂_veh^∧` mount column and §7.1 does not. §7.2's own result is that mount observability through a velocity constraint has **gain equal to forward speed**, and at a standstill that gain is zero: there is no direction of travel for a mount angle to be measured against. Keeping the column would let a stop shrink the mount covariance in proportion to the filter's own *velocity error* — precisely the quantity ZUPT exists to remove — which is information the stop does not contain. The mount block is where a wrong covariance is least visible and most expensive: it is read by the χ² gate, the uncertainty ellipse and the matcher's emission σ, and D-048 already leaves it with no process noise to re-inflate it. | S |
| D-052 | 2026-08-31 | **`update_zaru` takes the raw gyro sample for the step: `update_zaru(gyro)`.** The previous no-argument signature is changed. Correspondingly, **gating stays with the caller for the whole update family** — the harness calls `is_stationary` / `nhc_is_valid` and then the update; neither is moved inside the filter. | §7.3's innovation is `z = ω̃ − b̂_g`, which needs `ω̃`. A no-argument `update_zaru` cannot form it, so the signature could not be implemented as declared — the interface was fixed before the derivation was applied to it. The gating half is the same shape of problem: `nhc_is_valid` needs yaw rate and lateral acceleration and `is_stationary` needs accelerometer variance over a window, and **none of those is a property of the state** — they are properties of the IMU stream, which the filter does not hold. Putting them inside would mean the filter storing a raw-sample buffer purely to re-derive what the caller already has, and would hide the one rule the error budget depends on: ZUPT and ZARU fire **together** at every detected stop (D-004). A stop where only one runs is a bug, not a tuning choice, and that is only enforceable where the detector is called. | S |
| D-053 | 2026-08-31 | **The filter is not yet consistent, and Gate 1 must not be declared on it.** Full-state NEES over the 60 s reference scenario is **29.90** against a 95 % band of **[16.843, 19.195]** (18 dof × 100 runs) — over-confident. Propagation-only (**18.287**) and propagation+ZUPT (**18.929**) are both **in band** and are asserted by tests; ZARU is the sole cause and the full-state assertion is **not** committed as a passing test. Resolving it is P-04's, together with `P₀`. | Measured, not inferred. Per-block NEES against 3.0 expected, 100 runs: propagation-only all blocks 2.8–3.2; ZUPT-only all 2.8–3.4; NHC-only 1.6–2.8 (under-confident, the safe direction, and an artefact of `R_NHC = 0.5 m/s` being model slack a noise-free simulation does not need); **ZARU-only gyro-bias 12.89**. Two causes, both measured. (1) `zaru_sigma = 1.0e-3` rad/s is below the gyro white noise D-045 measured — `gyro_arw / √dt = 4.11e-4/√0.1 = 1.30e-3` rad/s at 10 Hz — understated 1.3× in σ, 1.7× in variance. Correcting it moves the gyro-bias block 12.03 → 8.48 and the total 29.90 → 23.71: real, and not the larger cause. (2) The same gyro sample is used as process noise inside `propagate()` *and* as ZARU's measurement, which the Kalman update assumes are independent; on a 60 s stationary run the gyro-bias block reads 3.41 with the correct σ but **2.74** given an independent second gyro read, and 2.72 when ZARU is decimated to every tenth step. Neither fix is P-03's to make: the first is tuning an `R` to pass a consistency test, which the phase forbids outright, and the second changes D-004's "at every stop" semantics that the error budget assumes. Recording the numbers and stopping is the cheaper failure — the buffer is the morning of 8 September, and an inconsistent covariance is invisible in a position plot. | S |

---

## Gate 0 — R-4, the CRSE convention (31 Aug 2026)

| ID | Date | Decision | Reason | Owner |
|---|---|---|---|---|
| D-054 | 2026-08-31 | **CRSE is `Σ\|eᵢ\|`, the sum of absolute per-second errors — a third convention, not either of the two D-023 chose between.** `CrseConvention.SUM_ABS` added, `crse()` gains an explicit branch per member with no `else` fallthrough, and `CRSE_CONVENTION` becomes `SUM_ABS`. `SUM_SQUARES` and `RMS` are retained so earlier working stays reproducible. **Supersedes D-023.** [EVALUATION.md](EVALUATION.md) §4.2 rewritten with the equations quoted verbatim; [GLOSSARY.md](GLOSSARY.md) and [eval/README.md](../eval/README.md) corrected to match. Closes R-4. | D-023 chose `SUM_SQUARES` on a consistency argument and labelled itself "reasoned, not verified", with seat D to pin it against the paper's equations before Gate 0. This is that check, and it went to neither option. R-WhONet (arXiv 2209.05877) **Eq. (16)** is `CRSE = Σ_{t=1}^{N_t} √(e_pred²)` — the root is taken **per term, inside the sum** — with **Eq. (17)** `CTE = Σ_{t=1}^{N_t} e_pred` as its signed counterpart, and `t` defined as a 1 s sampling period. The original WhONet paper (arXiv 2104.02581 §3.2) carries the same metrics at the same equation numbers. Both papers' *prose* says "cumulative root mean squared", which the equation contradicts and which is why three documents here described it as an RMS; the equation wins. Confirmed against WhONet's own published tables ([DATASETS.md](DATASETS.md) §3, four outage lengths × two methods): dividing by `N_t` leaves the per-epoch error rate flat to **1.4 %** (physics) and **2.3 %** (WhONet), where `√N_t` spreads 2.4× and leaving it as-is spreads 5.9×. D-023's argument never discriminated — `SUM_ABS` satisfies the 180 s row too (13.67/180 ≈ 0.076 m per epoch beside a signed sum of 2.90 m) — so it took all four rows. Pinned by `test_only_sum_abs_makes_the_published_tables_consistent`, so the reason is executable rather than a comment. **Nothing is invalidated:** no CRSE had been computed on real data, because the filter was `NotImplementedError` until P-03 and the dataset is not in this container. | D |

---

## Sprint 1 — P-04, `P₀` per block and the ZARU consistency fix (1 Sep 2026)

| ID | Date | Decision | Reason | Owner |
|---|---|---|---|---|
| D-055 | 2026-09-01 | **`P₀` is built per block by `core.reference.inekf.initial_covariance`, every entry traceable to a document section.** σ = 3 m position, 4.24 m/s velocity, 1.31° roll/pitch, 14.56° yaw, 42 °/hr gyro bias, 0.34 mg accel bias, 5° mount. Diagonal. `FilterConfig` gains `gnss_sigma_m` and `gnss_epoch_s` as the two inputs the harness overrides per fix. Table and derivations in [ERROR_BUDGET.md](ERROR_BUDGET.md) §10. Closes R-3. | The flat `1e-3·I` was one number, σ = 0.0316, carried in six different units, so it was wrong in every block: 95× too tight on position, 134× on velocity, 8× on yaw, 2.8× on the mount, and 43× too *loose* on gyro bias. Too tight is the direction that costs — a `P₀` below the truth makes the χ² gate reject good measurements, and that reads as a sensor fault rather than as tuning. Two entries need their reasoning stated rather than assumed. **Roll/pitch is `√(zupt_accel_var_thresh)/g = 1.31°`, not the 0.019° the measured 0.34 mg accel bias instability would allow:** the sensor floor is what the accelerometer *could* resolve, while the detector's own admission threshold is what actually bounds an alignment done at a detected stop. **The mount block is 5°, from §5's knock and not §5's ~1.15° requirement:** nothing has estimated `R_sv` (the PCA initialiser is P-11, `mount_rw` is zero per D-048), a prior at the requirement would have the filter open by asserting the budget is already met, and it would let the gate reject the NHC updates that would correct a real misalignment. Side effect, flagged rather than silently fixed: `test_propagate_linearises_at_the_midpoint_not_the_step_start` asserts a *ratio* against `max|P|` and so silently depended on the flat prior; it now sets `P = 1e-3·I` itself, which keeps its measured 3.7% separation and removes a coupling to a constructor it is not about. **The plan's R-3 row calls the position block "too loose"; it is too tight, by 95× in σ.** | S |
| D-056 | 2026-09-01 | **`zaru_sigma` is computed, not typed: `gyro_arw · √imu_rate_hz` = 1.2997e-3 rad/s.** `GYRO_ARW_MEASURED` and `IMU_RATE_HZ` become module constants and `FilterConfig` gains `imu_rate_hz = 10.0`. The tripwire `test_zaru_sigma_is_below_the_gyro_noise_the_repo_measured` has fired and is replaced by `test_zaru_sigma_is_the_gyro_white_noise_the_repo_measured`, which pins the identity instead of the defect. Closes the first half of D-053. | ZARU's innovation is `z = ω̃ − b̂_g` (SE23_PROPAGATION.md §7.3), whose noise *is* the gyro white noise per sample, so `R` is a measured sensor quantity and not a free parameter. Sourcing it is not the "tuning an R to pass a consistency test" that P-03 was forbidden — that prohibition is against picking a number because a test goes green, and this number was measured before the test existed. The 1.0e-3 it replaces predated the Allan run and was understated 1.3× in σ, 1.7× in variance, over-confident. Deriving rather than typing it removes the one failure this cannot survive: a `gyro_arw` update that leaves `zaru_sigma` behind, which no test can see because both values are individually plausible. Measured effect on the gyro-bias block, 100 runs, 3.0 expected: 13.14 → 9.32. Real, and **not** the larger cause. | S |
| D-057 | 2026-09-01 | **`update_zaru` is χ²-gated and returns `bool`, like `update_gnss`. `update_zupt` is deliberately not gated.** This supersedes D-053's attribution of the residual over-confidence. Measured gyro-bias-block NEES, 100 runs: ungated at 1.0e-3 **13.14** → ungated at the measured σ **9.32** → gated **3.05**, in band; full ZUPT+ZARU **19.03**, in band. | D-053 attributed the over-confidence to `zaru_sigma` plus the correlation between the gyro sample used as process noise in `propagate()` and the same sample used as ZARU's measurement, and named the correlation the larger cause. **Measured, it is not:** supplying a genuinely independent second gyro read moves the block only 9.32 → 8.22. The actual cause is one false ZARU per stop. `is_stationary` averages the gyro norm over a 0.5 s window, so at the step where the vehicle pulls away the window still holds four stopped samples and one moving one and the detector still says "stopped"; ZARU then fires against a true yaw rate of 0.05 rad/s and injects ~7.5e-4 rad/s into a bias estimate whose σ is 1.6e-4. That single sample carried a χ² distance of **1456** against the repo's own 3-dof 99% threshold of 11.345, so nothing subtle is being discriminated. Measured rejection rate is 2.0% of offered updates — the one transition sample per stop plus the ~1% tail the threshold is defined to reject. **Two alternatives were measured and rejected.** (1) *Fixing the detector* by testing the window maximum instead of its mean also works (13.14 → 3.38) but breaks a belief the repo holds on evidence: `test_a_vibrating_cabin_is_stationary_but_not_quiet` and D-045's finding that a real idling cabin carries 20–30× the mechanical energy of a bench-quiet segment. A detector that refuses to ZUPT at a genuine idling stop throws away the cheapest yaw-drift mitigation we have, so the mean stays and the gate catches the lag. (2) *Gating ZUPT as well* makes it worse, not better: ZUPT-only NEES goes 18.93 → **44.74**, because `zupt_sigma` = 0.02 m/s collapses the velocity block until legitimate innovations fall outside the gate and are locked out permanently. The asymmetry is physical: at the first moving sample the vehicle's *speed* is necessarily near zero, so a false ZUPT is nearly harmless, whereas its *yaw rate* is whatever a driver turning the wheel while pulling away chooses, so a false ZARU is not bounded at all. **D-004 is narrowed, not reversed:** ZUPT and ZARU are still both *offered* at every detected stop, and a stop where only one is offered is still a bug; what changes is that ZARU may decline. | S |
| D-058 | 2026-09-01 | **The full-state NEES assertion is committed one-sided — "not over-confident" — and `R_NHC` is not tuned.** Measured 14.14 against [16.843, 19.195]: under-confident. Propagation, ZUPT, ZARU and ZUPT+ZARU are all asserted two-sided and all in band. | The over-confidence that blocked Gate 1 is gone and the sign has changed, which is the safe direction. What remains is NHC alone — velocity block 1.43, mount block 1.66, every other block within 0.3 of 3.0 — because `nhc_sigma_lateral = 0.5` m/s is model slack for suspension travel, road camber and tyre slip, and the scenario contains none of them: its own NHC residual is 5.4e-4 m/s, so `R_NHC` is 1300× wider than this simulation needs. It **is** tunable into the band and that is exactly why it is refused: a sweep at 100 runs each gives 14.25 / 14.14 / 14.39 / 14.78 / 15.77 / **17.24** for `R_NHC` = 1.0 / 0.5 / 0.3 / 0.2 / 0.1 / 0.05 m/s, so 0.05 passes two-sided — and it would be a measurement-noise covariance fitted to a scenario that omits the physical effect the covariance exists to model, then carried onto real data where the effect is present. Nor is the curve safe below that: at the scenario's own 3.8e-4 residual the filter reads **14903**, wildly over-confident. `R_NHC` is P-10's, set from data by the adaptive head. Asserting one side commits the claim we can defend and still fails loudly on any regression to over-confidence. | S |
| D-059 | 2026-09-01 | **R-8 — the IO-VNBD accelerometer sign convention — is NOT closed, and SE₂(3) test 2 must not be read as a gravity-sign check until it is.** No substitute check was invented in its place. | R-8 requires reading real bytes from a stationary segment (`S-T2[31422:36490]` or `S-T7[47809:52285]`) and reporting the measured mean specific-force vector. **The dataset is not reachable from this container.** Evidence, not assumption: every CSV in `github.com/onyekpeu/IO-VNBD` is Git-LFS-tracked (`.gitattributes`: `*.csv filter=lfs`), a shallow clone yields 134-byte pointer stubs, and the LFS batch endpoint returns `access denied by the git proxy: onyekpeu/IO-VNBD is not in this session's authorized repository set`. Under G-4 the honest outcome is to leave R-8 open and say so rather than assert a convention from Android documentation, which is precisely the ambiguity R-8 exists to resolve — `TYPE_ACCELEROMETER` and a raw IMU log do not always agree. Consequence, stated because it is a Gate 1 dependency: SE₂(3) test 2 ("stationary 60 s → drift < 1 mm") remains a *self-consistency* check of `propagate_nominal` against `GRAVITY_NED`, not a check that the dataset's sign matches it. Half an hour of work on a machine that has the files, and it invalidates a Gate 1 result if skipped. | S |

---

## Sprint 1 — P-05, the two Gate 1 baselines (1 Sep 2026)

| ID | Date | Decision | Reason | Owner |
|---|---|---|---|---|
| D-060 | 2026-09-01 | **Both baselines live in [`eval/baselines.py`](../eval/baselines.py), return a `BaselineTrajectory`, and compute no metric of their own.** Scoring is a single `score()` that calls `eval.metrics.core.evaluate_outage` and nothing else. | A baseline that computed its own drift-% would be a second definition of the graded quantity, sitting in a file nobody reads as carefully as `eval/metrics/core.py`, and the two would diverge on the first change to either. Gate 1 is a *ratio* between a baseline and the filter, so a baseline computing its numbers a different way makes the gate partly a comparison of two metric implementations. | P |
| D-061 | 2026-09-01 | **The naive strapdown baseline mechanises with `core.reference.inekf.propagate_nominal`** — the same exact Γ₀/Γ₁/Γ₂ step the filter propagates with (D-029) — rather than with its own Euler integrator. It is the only place in the repository where acceleration is double-integrated, and every name, docstring and caption says "naive strapdown". | H-2's stated exception exists to *measure* what double integration costs, and the measurement is only about the aiding if the arithmetic underneath is identical. A baseline with its own integrator makes every four-trajectory figure partly a comparison of two mechanisations, and the difference we claim — constraints, learned speed, a running filter — would be contaminated by an integration-scheme difference nobody had measured. Reusing `propagate_nominal` also means the baseline inherits a step that is already checked against the derivation in CI. Pinned by a test against the budget's own arithmetic: 10 mg of accel bias gives 0.5·b·t² = **176.5 m** over 60 s, and the closed-form tilt-coupling case `−g·w·t³/6` = **−44.13 m** at w = 1e-3 rad/s over 30 s, measured to 4.5e-5. | P |
| D-062 | 2026-09-01 | **The GNSS-available baseline is a zero-order hold of the last `S-` fix, never an interpolation, and it refuses to hold a fix backwards into an epoch that preceded it.** | A receiver that has not updated has not moved its answer; interpolating manufactures positions the instrument never reported, which is the same objection `eval.loaders.truth.TruthTrack.index_at` already makes at length for the truth side. Reaching *forward* to a later fix is worse than a modelling error — it is a leak in time rather than in columns, and just as invalidating, so it raises. **Read with `GNSS_MEDIAN_INTERVAL_S = 9.0`:** across a 60 s window this baseline holds its answer for eight epochs of every nine and then steps, so its CTE and CRSE are dominated by the staircase and are a statement about the *update rate*, not about GPS accuracy. Its drift-%, measured at the window boundary, is the meaningful number, and it is phase-sensitive: a synthetic 60 s window opening 3 s after a fix and closing on one scores 5.0% with GNSS fully available. That is the honest Gate 1 denominator and it must be quoted with that sentence attached. | P |
| D-063 | 2026-09-01 | **Every heading in the harness — estimate and truth alike — is course over ground computed from consecutive positions by one shared `course_over_ground`, and a displacement under 1 cm per epoch holds the previous heading instead of reading `atan2` off noise.** | The truth track's allowlist is latitude, longitude and time of day and nothing else, deliberately (`eval.loaders.truth.TRUTH_COLUMNS`) — so a course from positions is the *only* heading ground truth can offer, and H-6's yaw error is only a like-for-like comparison if the estimate's heading is derived the same way. The 1 cm floor is not a tuning constant: `atan2` of a sub-centimetre displacement is a uniformly-distributed angle, so without it a yaw RMSE reports a large heading error at exactly the moments — stops — when heading is least at risk. 1 cm over a 1 s epoch is 0.01 m/s, two orders below `FilterConfig.nhc_min_speed`, which is this repo's existing statement of the speed below which direction of travel stops meaning anything; a test pins that relationship rather than the bare number. **Consequence to state in the write-up:** the filter's own attitude estimate is *not* what its yaw error is measured against — the course it actually made good is. Those differ whenever the vehicle's velocity is not along its heading, which is precisely what NHC asserts does not happen. | P |
| D-064 | 2026-09-01 | **The naive strapdown baseline is initialised from ground truth — perfect heading and speed at the outage boundary.** | Deliberately generous to the baseline. The drift it then reports is a *floor* on what no-aiding costs, so no part of the comparison rests on the baseline having been set up to fail — the objection a judge reaches for first when a paper's baseline looks bad. There is also no roll or pitch channel in a lat/lon truth track, so a level assumption is the only coarse alignment available, and it is the standard one. | P |

---

## Sprint 1 — P-07, the harness wired end to end (1 Sep 2026)

| ID | Date | Decision | Reason | Owner |
|---|---|---|---|---|
| D-065 | 2026-09-01 | **`eval/run.py` runs one filter pass per (sequence, outage length), driven by `mask_gnss`, and scores three trajectories per window — `filter`, `strapdown`, `gnss_available` — through the one metrics path.** Artefacts are `eval/figures/summary.json` and `eval/figures/windows.csv`, both carrying the stamp **inside** the file. | One pass per length rather than per sequence because the GNSS mask differs between lengths and a single pass cannot have GNSS both open and closed at the same sample. Three trajectories per window because Gate 1 is a ratio and the naive strapdown is the fourth line of every mandatory figure, so producing them in separate runs would let them drift onto different windows. The stamp goes inside the artefact and never into a filename (H-5): a filename is renamed, copied and pasted into a slide, and the provenance is lost at the first of those. Measured throughput of the Python reference is **3072 propagate steps/s** (0.326 ms/step), so the whole 14-sequence × 5-length sweep is ~5 minutes — no reason to cache intermediate passes, and P-14 has its hotspot. | D |
| D-066 | 2026-09-01 | **The `S-` and `V-` clocks are reconciled by the *median* of `seconds_of_day(date) − time_since_start_ms/1000` over all fixes, and `score()` now takes the window start on the truth clock explicitly.** `eval.run.truth_clock_offset_s`. | The two streams carry different clocks — `S-` counts milliseconds from the start of the recording, `V-` counts seconds since midnight — and `eval/loaders/truth.py` anchors its aliases specifically so neither can be mistaken for the other. `score()` originally derived the truth time as `start_idx / 10`, which is right for a synthetic track whose origin is zero and wrong for every real one: it would have graded each window against a stretch of road hours away, and `TruthTrack.index_at` would have raised — loudly, but for a reason that reads as a pairing failure rather than a units bug. Median over all fixes rather than the first, because a single anchor makes a whole sequence's alignment depend on one row's timestamp. This is item one in Gate 1's fixed diagnosis order (**timestamps**) and it is now pinned by a test whose fixture puts the two clocks 09:30:00 apart on purpose. | D |
| D-067 | 2026-09-01 | **A sequence whose sample interval strays more than 50 ms from the nominal 100 ms, or whose clock goes backwards, is skipped with a named reason rather than integrated; so is one whose paired `V-` track fails `align_to_sequence().is_usable`. Skipped stems are listed in `summary.json` and their count is printed.** | `eval/outages/inject.py` tiles outages in *sample indices* and the epoch times are `index / 10`, so a stream off the 10 Hz grid puts every epoch boundary somewhere other than where the truth lookup goes to find it — and the run still produces numbers. Five `S-` stems restart their clock mid-recording (D-013); integrating across the break is silent. A `V-` track that does not describe the same drive is the same class of failure. In all three cases the honest output is a smaller result set with the omission stated, never a number from a partial set presented as the whole: under-reporting the count is a far worse failure than a small count. | D |
| D-068 | 2026-09-01 | **The GNSS-available baseline returns `None` — not an exception, and not a dropped window — where an outage window opens before the receiver's first fix.** | An ordinary condition near the start of a recording. Raising would drop the window for the filter and the strapdown baseline too, which is a silent change to the sweep the protocol defines; fabricating a position would be worse. `None` means "this baseline has nothing to report for this window", the other two are scored normally, and the per-length window counts in `summary.json` then differ between methods — which is visible, and is why `gate1_ratio` carries `n_windows_filter` and `n_windows_gnss_available` alongside the ratio. | D |
| D-069 | 2026-09-01 | **Gate 1 cannot be measured in this container, and no number is reported in its place.** The harness computes the ratio, `summary.json` carries it, and running it here exits 2 at "dataset not found". | The IO-VNBD CSVs are Git-LFS objects and the LFS lane is credential-gated for this session (evidence in D-059). What is established instead is that the *wiring* is correct, which is the failure class that produces a plausible number rather than an error: `tests/test_harness_wiring.py` drives the whole path — loader arrays, both clocks, the GNSS mask, epoch slicing, the truth lookup, all three methods, the metrics composition and the stamped artefacts — on a synthetic drive whose answers are known independently, including the budget's own 176.5 m accelerometer-bias case reached through the full pipeline rather than through hand-built arrays. **Gate 1 remains open.** Under H-4 no learned component may be added until it closes, and closing it needs a machine with the dataset. | D |

---

## Sprint 1 — P-06, the Onyekpe reproduction's hyperparameter audit (1 Sep 2026)

| ID | Date | Decision | Reason | Owner |
|---|---|---|---|---|
| D-070 | 2026-09-01 | **The Onyekpe INS reproduction has nine settings the papers state and nine they do not, and the nine silences are recorded as ours before anything trains.** [`models/train_baseline_rnn.py`](../models/train_baseline_rnn.py) carries the table; `python -m models.train_baseline_rnn --audit` prints it and trains nothing. Omitted: epochs, early stopping/patience, train/validation split, weight initialisation, gradient clipping, LR schedule, shuffling and window stride, the magnitude of the GNSS input noise the paper says is injected, and the scaler fitting set (D-071). **None of the nine has a value yet** — they are listed as TBD, not filled in. | Half the reproduction is ours, which is the fact a comparison against a published number has to be read with. The papers also do not agree with each other: the INS paper does not surface its loss or its optimiser, and both rows come from WhONet — recorded per-row rather than smoothed into "the papers". Filling the nine in silently is the specific failure this table prevents: they would become constants in a config file, indistinguishable a week later from values someone tuned until the number matched. Reporting the gap is the deliverable — a reproduction landing 20% off with a stated reason is a result; one landing exactly on the published number with no explanation is usually a leak. A test asserts that every omission carries a decision id **and** that no stated value is ever relabelled as ours, which is the more dangerous direction. | M |
| D-071 | 2026-09-01 | **The set the 0–1 feature scaler is fitted on is called out separately as a leakage risk, not merely as an omission.** | It is the one silence that can invalidate the result rather than merely shift it, and it is invisible to every guard this repo has: fitting the scaler on train+test leaks the held-out set's dynamic range into training, and **no disallowed column is involved**, so `eval/loaders/columns.py` and the CI leakage job both stay green. The published text says "features scaled 0–1" and does not say on what. It must be fitted on the training split alone, and the fitted ranges must be recorded alongside the weights so the choice is checkable after the fact. | M |
| D-072 | 2026-09-01 | **P-06 training is NOT implemented, and `main()` raises rather than returning a plausible number.** | Blocked twice: the nine choices in D-070 are unresolved, and the IO-VNBD files are Git-LFS objects this container cannot fetch (D-059), so there is nothing to fit. A stub returning an untrained model's loss would produce a number that looks like a reproduction, which is worse than an error. `torch` is imported inside `main` rather than at module scope specifically so the audit and its tests run in the CI job that has no `ml` extra — the harness is the critical path and must never block on a 2 GB download. | M |

---

## Sprint 3 — P-11, `R_sv` in-filter, the PCA initialiser and the bump detector (1 Sep 2026)

| ID | Date | Decision | Reason | Owner |
|---|---|---|---|---|
| D-073 | 2026-09-01 | **`pca_mount_yaw` initialises `R_sv` from horizontal specific force and returns the *spread* alongside the angle; `initial_covariance` takes that spread for `P₀`'s mount block.** Spread is `√(λ₁λ₂ / (n_eff(λ₁−λ₂)²))` with `n_eff = n(1−ρ)/(1+ρ)` from the measured lag-1 autocorrelation. | An initialiser that returns only an angle forces the caller to invent an uncertainty for it, which is exactly the hole D-055 had to fill with a stated 5°. The formula is the asymptotic standard error of a 2D principal-axis angle and it is **checked by Monte Carlo** rather than recalled — within 7% over eigenvalue ratios 2–25 and n from 200 to 5000. `n_eff` rather than `n` because 10 Hz accelerometer samples are strongly serially correlated and using the sample count understates the spread by the square root of the correlation time; the AR(1) correction is derived, and labelled as derived, in the spirit of D-045's Gauss–Markov bias noises. | S |
| D-074 | 2026-09-01 | **PCA's 180° ambiguity is reported, never guessed.** `MountInit.sign_resolved` is False unless a `forward_reference` — a per-sample signed scalar that grows with forward acceleration, e.g. finite-differenced `gps_speed_kmh` — is supplied to break it. | A covariance cannot distinguish forward from backward: they share a principal component exactly. A 180° mount error is not a large version of a small one — it is a vehicle driving backwards — and it will **not** converge out under NHC, whose lateral and vertical rows are satisfied just as well by a reversed forward axis. So it must be resolved by something outside the covariance or declared unresolved. Guessing it would be the single most expensive silent error the initialiser could make. | S |
| D-075 | 2026-09-01 | **`mount_disturbance_gyro_thresh = 3.0` rad/s cannot detect the 5° knock the detector exists for, and the threshold is NOT lowered to make a test pass. Recorded as an open gap.** | Closed-form: a knock of angle θ delivered inside one sample presents at most `θ/Δt` of measured rate, because the stream is 10 Hz and the transient is averaged away below Nyquist. So 3.0 rad/s corresponds to `3.0 × 0.1 = 0.3` rad = **17.2°**, while [ERROR_BUDGET.md](ERROR_BUDGET.md) §5's 5° knock presents 0.87 rad/s — measured at 0.875 in the P-11 scenario, and the detector does not fire. Lowering the threshold until the synthetic knock trips it would be fitting a detector to a scenario, and the threshold has no source in this repo to fit it to instead. What it needs is a recording of a real phone being knocked in a cradle, which is ten minutes of seat A's time and does not touch the Gate 1 critical path. Until then: the re-inflation mechanism is exercised by signalling the knock directly, the detector guards only a violent one, and the ~1° requirement in §5 rests on the PCA initialiser plus unaided NHC. A test asserts the blind spot and its arithmetic, so resolving it will fail loudly rather than quietly. | S |
| D-076 | 2026-09-01 | **`reinflate_mount` *adds* variance to the mount block and leaves the rotation alone.** Default 5°, from §5. | Two choices in one line, both the safe direction. **Adds** rather than resets: a reset to a fixed value would *shrink* the covariance of a filter that had already lost track, and that is the direction from which nothing recovers. **Leaves the state alone** because a knock is news about uncertainty, not a measurement of a new angle — the gyro energy that detected it does not say how far the phone turned. NHC then re-estimates `R_sv` from the widened prior with gain equal to forward speed (§7.2), which is why this works on a motorway and barely at all in a car park. Measured over 60 s at 15 m/s after a 5° knock: 0.43° residual with re-inflation against 8.56° without — worse than the knock, because a stale mount behind a confident covariance drags the rest of the state with it. | S |

---

## Sprint 3 — P-14, the `core/ffi/` interface definition (1 Sep 2026)

| ID | Date | Decision | Reason | Owner |
|---|---|---|---|---|
| D-077 | 2026-09-01 | **The FFI surface is a C header, [`core/ffi/idr_core.h`](../core/ffi/idr_core.h)** — not a C++ header and not a Rust trait — and it ships as a definition with no implementation behind it (D-043). | Both frontends have to bind to the same library: JNI binds C directly, and Rust reaches C through `extern "C"`. A C++ or Rust-native interface would force one of the two through a shim, and a shim is where the units and the ownership rules get quietly re-interpreted. C also makes the "one library, two frontends" claim in [core/README.md](../core/README.md) checkable rather than aspirational. The header carries units, frames, buffer ownership and threading on **every** entry point, and cites the decision each part of its shape comes from — the port happens in October, from this file, probably by someone who was not in these conversations, and a constant with no reason attached is a constant that gets "simplified". A test asserts the header stays an accurate description of the Python reference: same error-state dimension, every config field present, and the two calls that can decline a measurement (`idr_update_gnss`, `idr_update_zaru`) reporting it while the two that cannot do not. | S |
| D-078 | 2026-09-01 | **The measured hotspot for the October port is the 36×36 Van Loan matrix exponential: 0.172 ms, 50.1% of `propagate`.** Python reference throughput is 2,905 `propagate`/s and **2,327 realistic 10 Hz steps/s = 233× real time**. The port must **validate** any cheaper closed form against this one rather than substituting it. | Measured, not estimated: `expm_series` is the largest single `tottime` in a cProfile of 600 steps (0.108 s of 0.226 s cumulative), and the per-call figures come from a timed loop after warm-up. Two consequences. (1) **Nothing in the harness needs optimising** — the full 14-sequence × 5-length sweep is about five minutes, so there is no argument for caching intermediate filter passes and no reason for the wiring to be anything other than the simplest correct thing. (2) At 200 Hz the FOG build's step budget is 5 ms and even the *Python* reference would use 8.6% of it, so **the port is not needed for throughput** — it is needed for the deployment target, which is a different and more honest claim than "the Python is too slow". The `expm` is not incidental cost: D-031 chose it over the `Φ G Q_c Gᵀ Φᵀ Δt` shortcut on a measured 4.2% error at Δt = 0.1 s, and an undersized `Q_d` makes the filter reject good measurements at the χ² gate — a failure that reads as a sensor problem rather than a tuning one. | S |

---

## Sprint 3 — P-13, the replay renderer (1 Sep 2026)

| ID | Date | Decision | Reason | Owner |
|---|---|---|---|---|
| D-079 | 2026-09-01 | **The renderer reads a versioned trajectory record (`idr-trajectory/1`) that `eval/run.py` writes, and every displayed number — including per-epoch drift-% and yaw error — is computed on the harness side.** The page refuses a record whose schema string it does not recognise. | D-039's rule is that the renderer contains no physics and no simulation, and "no physics" has to mean something enforceable. If the page computed drift-% it could display a number the evaluation never produced — and it would agree with the harness for a while, which is exactly how that error survives review. Computing them in `trajectory_record` makes the page a *view*. The schema string exists because a renamed field renders as `undefined` and draws nothing where a trajectory should be, which is easy to miss on a slide; refusing an unknown layout fails loudly instead. Tests assert both directions: every field the page reads is one the harness writes, and the page contains no `Math.random`, no `Math.atan2`, and no division by `distance_m`. | A |
| D-080 | 2026-09-01 | **A missing artefact shows an empty state. There is no fallback that generates data, and the page makes no network request beyond same-directory harness output.** Over `file://` it does not fetch at all and offers a file picker instead. | The single failure the frozen protocol exists to prevent is a cockpit fed by its own generator (D-039, D-042). The `catch` around the fetch creates nothing, and a test asserts that. No CDN, no font, no map SDK, no analytics: a demo that needs the network cannot back a 100%-offline claim (D-041), and a judging venue's wifi is not a dependency worth having. `file://` cannot read sibling files by fetch in any current browser, so the picker is the offline path rather than a workaround — it keeps "opens offline from `file://`" true without weakening the no-fetch rule. | A |
| D-081 | 2026-09-01 | **The replay window is the *first* 60 s window of each mandatory plot sequence, not a chosen one, and every sensor caption names the stream and the rate.** | Picking the window that plots best is the same error as picking the sequence that scores best, and it is harder to spot because a figure has no sample size printed on it. 60 s because it is the reference case the whole error budget is sized against ([ERROR_BUDGET.md](ERROR_BUDGET.md) §1). The caption reads "`S-` smartphone stream at 10 Hz" and says in as many words that the 200 Hz FOG configuration is **not** demonstrated on this dataset — we cannot show a 200 Hz pipeline on IO-VNBD and we will not appear to. Verified in a browser against real harness output: the page loads, the stamp renders on the page with a `NOT REPRODUCIBLE` marker for a dirty tree, and all four trajectories, the covariance ellipse, the scrubber and the traces draw. | A |

---

## Sprint 3 — P-15 and P-16, the write-up and the submission audit (1 Sep 2026)

| ID | Date | Decision | Reason | Owner |
|---|---|---|---|---|
| D-082 | 2026-09-01 | **[METHOD.md](METHOD.md) tags every quantity with one of four provenance classes — `[measured]`, `[verified]`, `[analysis]`, `[evaluated]` — and states in §0 that no `[evaluated]` number exists.** | D-042 says no performance figure enters the write-up unless `eval/run.py` produced it, stamped. Applied literally that would have forced a choice between dropping the whole derivation and analysis or presenting, say, a NEES figure as though the harness had produced it — because they are all "numbers" and the rule has one bucket. The four classes keep D-042 exactly as strict where it bites (§12 is empty and says so in its first line) while letting the document quote the Allan run, the closed-form budget arithmetic and CI-verified code properties **with their sources named**. The property that matters: a reader can tell at a glance that **no performance claim is being made**, which is a stronger position than a table of numbers with an asterisk. | C |
| D-083 | 2026-09-01 | **The submission audit is written and run seven days early, against an incomplete submission, and reports six of eleven items as open.** [SUBMISSION_AUDIT.md](SUBMISSION_AUDIT.md). | An audit run the night before is an audit that can only rubber-stamp. Run now, it produced three things a later one could not have: leakage pass #2 is **not** closed (the guard is verified but no live feature path exists to audit, and saying "met" would have been the exact softening the phase forbids); the portal cut-off has been unconfirmed since 27 Aug and is the only open item that can invalidate the *schedule* rather than a number; and the deferred list was checked **in both directions** — nothing claimed as built is absent, which is the direction more often wrong. The audit's own summary line is the honest position: the submission as it stands is a defensible engineering artefact with no performance claim, which is worse than one with a measured Gate 1 and better than any version that quotes a number nobody can regenerate. | C |

---

## CI — the SO(3) orthogonality bound (1 Sep 2026)

| ID | Date | Decision | Reason | Owner |
|---|---|---|---|---|
| D-084 | 2026-09-01 | **`test_reorthonormalisation_keeps_r_on_so3`'s bound moves from an absolute `1e-15` to `32 × machine epsilon`, and a second test is added that measures what the bound discriminates against.** G-3 requires a row before a test changes; this is it. | **The failure is not this PR's** — CI run 33480227380 on `main` at `b856046`, the PR's own base, fails the same test with the byte-identical value `1.2212453270876722e-15`. `state.R` after 1000 steps is bit-identical under the old flat `P₀` (checked), so nothing in this branch reaches it. **The test's belief was wrong, not the code.** `orthonormalise` is an SVD round-trip, so `RᵀR − I` is O(n·eps) with a constant set by the runner's LAPACK build: measured **3.00 eps** on numpy 2.4.6 and **5.50 eps** on numpy 2.5.2. The old `1e-15` is **4.50 eps** — it sits *between* those two, so the assertion was a coin flip on the BLAS the runner shipped, which is exactly why py3.10 was green and py3.12 red on the same commit. This is **not** loosening a tolerance to get green: the new bound is 5.8× above the worst residual observed and 11× *below* the un-orthonormalised drive, which the new `test_reorthonormalisation_is_what_keeps_r_there` measures at **349 eps** — a 116× separation that the old test asserted a number about but never demonstrated. A tolerance nobody has shown to separate anything is not a tolerance, so the pair is strictly stronger than the single assertion it replaces. | S |
| D-085 | 2026-09-01 | **R-8 is closed. The IO-VNBD `S-` accelerometer is specific force with gravity included, in a phone frame whose `GRAVITY` channel points UP, and the repo's existing convention is correct as written — nothing is flipped.** Supersedes D-059, which left R-8 open for want of the files. SE₂(3) test 2 may now be read as a gravity-sign check. | Measured on the two segments D-059 named, read `latin-1`, on a machine that has the dataset. **Stationary means** (`S-T2[31422:36490]`, 5068 samples, 506.7 s, gyro-norm mean 0.00100 rad/s): `[-0.4250, +0.1190, +9.7461]`, ‖·‖ = **9.7561**; (`S-T7[47809:52285]`, 4476 samples, 447.5 s, gyro-norm mean 0.00130): `[-0.4856, -0.3070, +9.8613]`, ‖·‖ = **9.8781**. Both ≈ g, so gravity is **included** — the channel is not a gravity-removed linear acceleration. The files also ship a separate `GRAVITY` channel with ‖mean‖ = 9.8066 on both segments, and `accel − gravity` is `[-0.0027, -0.0009, -0.0506]` and `[-0.0026, +0.0003, +0.0716]` — i.e. the two channels are parallel and agree to 0.05–0.07 m/s², which is AndroSensor's `TYPE_ACCELEROMETER`/`TYPE_GRAVITY` pair and not a raw IMU log. **A stationary segment cannot see the remaining ambiguity**, because a convention that negates accel and gravity together leaves every quantity above unchanged, and `pca_mount_yaw` takes `up = gravity/‖gravity‖` on trust. It was resolved against the gyro, which the flip does not touch: over turning epochs (speed > 15 km/h, \|ψ̇\| > 3 °/s) the correlation between GPS heading rate and `ω·ĝ` is **−0.9860** on S-T2 (n=725) and **−0.9902** on S-T7 (n=992), with fitted slopes **−0.017764** and **−0.017063** (rad/s)/(deg/s) against an ideal π/180 = 0.017453 — within 1.8 % and 2.3 %, so the projection is a true yaw rate and not merely a sign that happened to match. Negative ⇒ a right turn's rotation vector opposes `ĝ` ⇒ **`GRAVITY` points up**. Consequence: at rest `R_sv @ accel` has `z ≈ −9.8`, which is the `−GRAVITY_NED` that `propagate_nominal` documents at [`core/reference/inekf.py`](../core/reference/inekf.py) line 191. No code changed. | S |
| D-086 | 2026-09-01 | **The `S-` `date` column does not contain the format its own header advertises, and `_S_DATE` is widened to accept the bytes as shipped.** Not a loosened pattern: the advertised spelling still parses, and a new test asserts each. | The header reads `DATE (YYYY-MO-DD HH-MI-SS_SSS)` and the pattern was written from that string. The values are `'2019-09-04 19:21:51:494'` — the sub-second separator is a **colon**, not `_` or `.`, and every value is wrapped in **literal single quotes** that the pattern's `$` anchor then refuses. Both defects had to be absent for a value to parse and neither was, so **no real `S-` file parsed at all**: `eval.run` raised `ValueError("no value in the 'date' column parses as a time of day")` on the first held-out sequence it loaded, which is why Gate 1 had never been measured on a machine that has the data. The end anchor still separates the time from the calendar date — `2019-09-04` is the same `\d{1,2}[-:]\d{2}[-:]\d{2}` shape once `:` is a permitted sub-second separator — and that is asserted rather than assumed. The existing test used the advertised spelling only, so the suite was green against a format the dataset does not use. | D |
| D-087 | 2026-09-01 | **Completes D-066. `truth_clock_offset_s` fixed the *clock*; the *origin* was still assumed. `evaluate_sequence`'s `t0` and the GNSS-available baseline's fix times both read `start_idx / SAMPLE_RATE_HZ + offset`, which is correct only where `time_since_start_ms` opens at zero — and it does not, for 11 of the 14 held-out stems.** Both now index an absolute per-sample clock built from the row timestamps. | D-066 removed `start_idx / 10` from `score()` and added the median-anchored offset, and it is why this run got as far as it did. What it did not reach is that adding the offset only relocates the clock; the relative time itself is still `start_idx / SAMPLE_RATE_HZ`, which silently assumes the recording opens at zero. The held-out files are excerpts that keep the parent recording's clock, so the assumption fails almost everywhere. Measured openings — Vta11 **1118.51 s**, Vta12 1181.51, Vta9 929.51, Vtb3 581.21, Vw16b 602.41, Vw17 721.11, Vtb8 **8052.91**, Vtb11 **8205.91**, Vw6 **12779.31**, Vw7 13185.41, Vw8 **13365.41**; only S3a (31.32), Vta1a (0.02) and Vw12 (0.01) are near zero. The epoch was therefore addressed by up to **3.7 hours**, ~16.8 km of road at motorway speed. As D-066 predicted for its own case, `TruthTrack.index_at` raised rather than interpolating, so it failed loudly — but again for a reason that reads as a pairing failure rather than a units bug. It survived D-066 because `synthetic_drive` builds `t_rel` as `arange(n)/SAMPLE_RATE_HZ`: the fixture that D-066 added to pin the two clocks 09:30:00 apart still opens its own relative clock at zero, so it could not see this half. The fixture now takes `t_rel_start_s`, and with the old expression restored the new test reproduces the production failure — `SYNTH: 2 of 2 epoch(s) have no truth sample within 0.05 s; worst is 1088.510 s` against Vta11's real 1088.019 s. | D |
| D-088 | 2026-09-01 | **Gate 1 is still NOT measured, and no ratio is reported — but the reason has changed from "no dataset" to "the long-outage set has one usable sequence".** Supersedes D-069's *cause* while leaving its conclusion standing. The harness now runs end to end on real bytes; what blocks the gate is truth pairing, and re-picking the split is seat D's call, not this session's. | On a machine that has the dataset, with D-086 and D-087 fixed, all 14 held-out stems were run and each one's blocking reason recorded. **Grading (5): Vta1a 1242 windows, Vw6 39, Vw16b 33, Vta12 12, Vta11 6.** Producing zero windows though usable: Vta9 (15.5 s, shorter than any outage), Vw17 (32.8 s). **Refused by `align_to_sequence` (6):** S3a **0/254 fixes matched** (the `S-` clock leads `V-` by **+3593.5 s**, an hour to within 6.5 s), Vw7 **0/18** (−167.8 s), Vw8 **0/17** (−161.0 s), Vtb11 median residual **35.1 m**, Vtb8 **33.7 m**, Vtb3 8.5 m but at a best-fit lag of **−2.70 s** against the 0.5 s limit. **Aborted the sweep (1):** Vw12, `TruthPairingError` — 3 of 11 epochs have no truth sample within 0.05 s, worst 2.305 s. **The consequence for the gate:** `LONG_OUTAGE` is `S3a, Vtb3, Vta1a`; S3a and Vtb3 are both refused, so the 60 s criterion would rest on **Vta1a alone** — and Vta1a is one of only three stems in the whole dataset whose GNSS runs at 1 Hz rather than 9 s (the 2026-08-29 P-01(2) finding), so it is the least representative sequence available to stand for the set. S3a is also two of the four mandatory plots. Under `eval/run.py`'s own rule — "reporting nothing is correct here, reporting a number from a partial set is not" — and D-067, no ratio is computed from the five that grade. **What this needs is the D-044 split re-pick, now with measured pairing evidence rather than a file listing**, plus a decision on whether a window lacking truth coverage is dropped (as the error text instructs) or fails the stem, since at present it fails the whole run and discards the 1260 windows already computed. | D |
| D-089 | 2026-09-02 | **A window whose epochs the paired `V-` track does not cover is dropped from the protocol, counted, and reported; it no longer aborts the sweep and it does not fail the whole stem.** `evaluate_sequence` catches `TruthPairingError` around the *whole* per-window body and appends nothing for that window; the count and the per-window reasons reach `summary.json` as `dropped_windows`/`n_dropped_windows` and are printed. Executes the second half of what D-088 left open. Supersedes nothing — it names behaviour no row had chosen. | Three options existed and only one was ever intended. **(c) today's behaviour** — `evaluate_sequence` raises through the per-sequence `except SequenceUnusable` in `main()` and kills the run — is the only one that destroys already-valid results: on the real sweep it discarded the **1260 windows** already computed for Vta1a, Vta11 and Vta12 because the 9th of 14 stems had a 2.305 s hole. Nothing chose that; it is the gap between `TruthPairingError`'s own instruction — *"drop the window from the protocol rather than interpolating across the gap"* — and the absence of any mechanism to drop one. **(b) failing the stem** is defensible but throws away evidence the instrument did record: Vw12's hole is 3 epochs of 11, and the other 8 windows are covered at 10 Hz by the same track that covers the graded stems. An alignment failure is a statement about the *pairing* and rightly kills the stem; a coverage gap is a statement about **three epochs**, and D-067's doctrine is "a smaller result set with the omission stated, never a number from a partial set presented as the whole" — which is a rule about *reporting* the omission, not about widening it. **(a) is what the error text already instructs**, and it is what is implemented. **Atomicity is the part that can go wrong quietly.** Three call paths inside one window can raise — `_strapdown_for` → `initial_state_from_truth` → `truth.ned_at`; `score` → `truth.displacements_ned`/`distance_m`; and `_replay_record` → `truth.displacements_ned`, which the handover's list omitted — so a `try` around any one of them would append `filter` and not `strapdown`, and the per-method window counts in `summary.json` would stop matching while every individual number stayed correct. The window's three results are therefore built into a local list and only merged once the whole window, replay record included, has succeeded; `test_a_dropped_window_never_leaves_a_partial_set_of_methods` asserts the counts stay equal across methods. **`_gnss_for` cannot raise it** — it reads `fix_idx`/`fix_ned`/`t_abs_s` and never touches the truth track — and is left alone. **One silent-failure mode is created by this change and closed with it:** before it, a hole inside a `MANDATORY_PLOT_SEQUENCES` replay window aborted the run; after it, the window is dropped and the mandatory figure would simply be absent. `eval/splits.py` says such a figure "fails the run rather than being noticed the night before" and **nothing in the code enforced that** — so `main()` now computes `mandatory_plots_missing` and prints it. It is reported, not made fatal, and that limit is deliberate: Vta11 is 51 s long and can never yield the 60 s `REPLAY_LENGTH_S` window, so a fatal check would suppress the Gate 1 number that the D-044 re-pick needs in order to be made at all. Whether it becomes fatal is seat D's, inside the re-pick. Nothing is invalidated: no sweep has produced a reported number. | D |
| D-090 | 2026-09-02 | **The D-044 split re-pick is NOT made here, and `LONG_OUTAGE`, `CHALLENGING`, `MANDATORY_PLOT_SEQUENCES`, `TRAIN`, `EVALUATION.md` §3 and `tests/test_protocol.py` are all left exactly as they stand.** The candidate pool is enumerated instead — `eval.loaders.truth.paired_stems()` — and `python -m eval.cadence --all-paired --data-root data` is the one command that produces the evidence the re-pick needs. The team lead's instruction to re-pick stands and is not refused; what is missing is the measurement, not the authorisation. | **The dataset is not on this machine.** `data/` holds `README.md` and `manifest/` and nothing else (224 KB); the container has no `.venv`, and a shallow clone of `github.com/onyekpeu/IO-VNBD` yields 132-byte Git-LFS pointer stubs whose batch endpoint returns *"access denied by the git proxy: onyekpeu/IO-VNBD is not in this session's authorized repository set"* — D-059's finding, reproduced verbatim today. The credentialed attach that would lift it was refused. So `align_to_sequence` cannot be run on a single candidate here. **The handover named this session's machine as the one that has the data; it does not.** Under G-2 and G-4 that makes every replacement stem unpickable: the instruction accompanying the re-pick is *"do not propose a replacement you have not aligned"*, and proposing one from the file listing alone is exactly the reasoning that produced D-044's defect in the first place — a split drafted from a file tree, which the first contact with real bytes then refuted for 6 of 14 stems. Picking now would also be picking blind against H-3's specific failure mode, since the only stems whose numbers are known are the ones that already pass. **What was established here, from the committed manifest rather than from bytes:** the synchronised folder holds **exactly 72 stems, every one of them shipping both an `S-` and a `V-` file** — so the pool is 72, not "roughly 72"; 14 are the held-out stems D-088 measured, 5 are `TRAIN` and barred from promotion by `assert_split_disjoint`, and **53 have never been through `align_to_sequence`**. That is the size of the outstanding measurement, and it is a number, not an estimate. **A reproducibility gap was found while looking for the prior evidence.** D-088's per-stem table is attributed to `scratchpad/r8/stem_status.py`, and `scratchpad/` is gitignored — the script is in no commit, so the table cannot be regenerated on any machine but the one that wrote it, against H-5 and §7.1. The replacement is not a new tool: `eval/cadence.py` was already tracked for precisely this reason ("the number that moves the protocol has to come from something committed, stamped and re-runnable") and already measures every field the re-pick needs — alignment residual, best-fit lag, `s_time_is_monotonic` (the `np.diff(t) < 0` reset check), `imu_duration_s` (which is what disqualified Vta9 at 15.5 s and Vw17 at 32.8 s) and `meets_1hz` (which is what makes Vta1a unrepresentative). It only lacked a way to name the pool, which `--all-paired` adds. Two stems in the pool are pre-disqualified on evidence already in `phases.md` §11 and are flagged by the sweep rather than silently dropped: `S3b` and `Y1` restart their clock mid-file (1 and 3 breaks), and `Vw1` and `Vw15` contain zero GPS position changes. | D |
| D-091 | 2026-09-02 | **The `S-` `date` column is UK *local* time and the `V-` VBOX clock is UTC, so the two are one hour apart for every recording made inside British Summer Time.** `seconds_of_day` stays the parser and keeps returning the value as shipped; a new `seconds_of_day_utc` applies the UK civil-time rule, and `truth_clock_offset_s` and `align_to_sequence` both read that instead. Corrects the belief stated in `seconds_of_day`'s own docstring — *"no timezone question arises: both streams were logged on the same vehicle on the same clock"* — which is true of the vehicle and false of the two loggers. Supersedes D-088's reading of S3a, Vw7 and Vw8 as unpairable. | **This is a frame conversion, not a fitted offset, and the difference is the whole justification.** No per-stem number is tuned and no threshold moves: the rule is the published UK rule (BST = UTC+1 from 01:00 UTC on the last Sunday of March to 01:00 UTC on the last Sunday of October), it reads only the calendar date each row already carries, and it commits to a prediction *before* any residual is computed. That prediction is falsifiable and was tested on twelve stems: **12 of 12.** Measured clock difference after D-087's correction — `S3a +3593.5 s`, `S1 +3600.5 s`, `S3c +3600.7 s`, against `Vta1a −13.6 s`, `Vw4 +0.3 s`, `Vtb3 −1.4 s`. The three at exactly one hour were recorded **2019-09-04, 2019-09-08 and 2019-09-04**, inside BST; every stem near zero was recorded **2019-11-06** or **2020-01-08**, outside it. Nothing was fitted to make that line up — the split is by calendar date alone. The remaining four BST stems do **not** land on a clean hour (`S2 +3794.8`, `S4 +7440.8`, `M +8029.0`, `Y1 +4306.4`), and that is the second half of the confirmation rather than a hole in it: they are **exactly** the four stems the 2026-08-29 P-01(2) finding lists as restarting their clock mid-file (S2 one break, S4 two, M one, Y1 three), and `truth_clock_offset_s` anchors on the **median** fix, which a reset moves onto a different recording. Seven BST stems, three reset-free and at 1.000 h, four reset-carrying and not — no exceptions in either direction. **What it recovers.** `S3a` — a `LONG_OUTAGE` member *and* one of the two surviving mandatory plots — was refused with **0 of 254 fixes matched**, and D-088 recorded it as a pairing failure; it is a one-hour clock bug. `S1` and `S3c` are `TRAIN` stems whose truth was equally unusable, which matters because all five TRAIN stems failed pairing and P-06/P-08 would otherwise train against no ground truth at all. It also restores **Driver A** to the reachable set: every one of the 29 stems the first sweep found usable is Driver E, and the only non-E stems in the synchronised folder (six `S`, one `M`, one `Y1`) are precisely the ones this bug refused — so without it the held-out split is single-driver by construction, and the domain-shift argument rests entirely on seat C's collection. **What it does not claim.** The one ambiguous local hour at the October transition cannot be resolved from a local timestamp alone, and is documented rather than guessed; no IO-VNBD recording falls in it. `align_to_sequence`'s contract is unchanged — it still *reports* the residual and refuses to subtract a misalignment away; what changed is that both sides are now on the same clock before it looks. `zoneinfo` was not used: it needs the `tzdata` package on Windows, which is the dev box, and G-5 forbids the dependency — the rule is implemented from the standard and pinned by hand-computed boundary tests. | D |
| D-092 | 2026-09-02 | **The held-out split is re-picked against measured truth pairing, and `TRAIN` is pinned to an explicit list of stems.** Executes the TODO D-044 opened and carried in `eval/splits.py` and EVALUATION.md §3 ever since. `LONG_OUTAGE` goes from `S3a, Vtb3, Vta1a` to **`S3a, S3c, Vta1a, Vta1b, Vta16, Vw2, Vw4`**; `MANDATORY_PLOT_SEQUENCES` from `S3a, Vta11` to **`S3a, Vw4`**; `CHALLENGING` loses `Vw17, Vta9, Vw7, Vw8, Vtb8, Vtb11` and gains nothing; `TRAIN` becomes nineteen named stems in place of five plus a prose clause. Two new invariants, `REFUSED_TRUTH_PAIRING` / `TOO_SHORT_FOR_ANY_OUTAGE` and `assert_no_family_straddles_the_split`. `tests/test_protocol.py::test_long_outage_set_matches_the_documented_protocol` changes under G-3, and this row is the one that precedes it. | **Every stem was measured before it was named**, over the whole 72-stem pool — `python -m eval.cadence --all-paired --data-root data`, artefacts stamped at `f4c7b1b` — through `align_to_sequence` *and* through the harness's own `imu_stream`/`assert_uniform_grid`, because pairing and integrability are separate gates: 35 of 72 pair, and only 31 of those integrate. That second gate caught `Vta17`, whose sample interval strays 1.184 s and which the first gate had passed. **Gate 1's 60 s criterion goes from 42 windows on one sequence to 459 on seven**, and from a stem that is one of only three in the dataset with 1 Hz GNSS to a set whose majority is the 9 s cadence everything else has. `Vw4` alone contributes 210. **Nothing was chosen for its score** — no filter has been run on any of these stems, so there is no number to have chosen by; the criteria are alignment residual, best-fit lag, monotonic clock, uniform grid and duration, all of them fixed before the sweep. **`S3a` returns because D-091 corrected a clock, not because a threshold moved:** it matched 0 of 254 fixes as a BST-vs-UTC hour, and pairs at 4.92 m once both streams are on the same clock. **`Vtb3` leaves** at a −2.70 s best-fit lag against a 0.5 s limit; the whole `Vtb` family carries a systematic +1.6 to +2.1 s lag, and `AlignmentReport`'s own docstring calls that "a finding about the dataset, and not something this module may silently subtract away" — so the limit was **not** widened to keep the wet-road group alive, and the group is empty as a result. **Promotion is confined to `LONG_OUTAGE`, where membership is measurable.** Nothing was promoted into `CHALLENGING`: every group name asserts what the vehicle was doing, the only source is the paper's list, and no document in this repo describes the 53 previously unallocated stems — so filling `wet_road` from them would be inventing the label, which is G-4. Thinned groups stay thin and the omissions are named. **`TRAIN` had to be pinned for the promotion to be safe.** EVALUATION.md §3 defined it as five stems "plus Vta/Vtb/Vw/Vfa/Vfb subsets not listed above"; `assert_split_disjoint` compares two tuples, so promoting `Vw4` out of that unnamed remainder would have overlapped train and test with no check in the repo able to see it. **A second and sharper leak follows from it:** lettered stems are segments of one parent recording, so `Vw14b` held out while `Vw14a`/`Vw14c` train is the same drive on the same road minutes apart. `assert_no_family_straddles_the_split` encodes that, and it **fired on its first run** against `Vw16a`(train)/`Vw16b`(test) — a leak this very re-pick had introduced. Consequences taken rather than argued away: `S3c` moved out of `TRAIN` to follow `S3a`, `Vta1b` joined the held-out set to follow `Vta1a`, `Vw14` stayed whole in train, and `Vw16a` is left unassigned, forgoing nine 10 s windows. **What this still cannot do**, recorded in EVALUATION.md §3 rather than discovered later: the **wet-road scenario cannot be reported at all**; the **roundabout plot cannot be produced**, because `Vta11` is 51.0 s and `REPLAY_LENGTH_S` is 60 s, which is why `Vta11` left the mandatory-plot set and is left open rather than fixed by shortening the replay window; and **driver diversity is thin** — 62 of 72 paired stems are Driver E, and `S3a`/`S3c` are the only Driver A sequences held out, present only because of D-091. **Nothing is invalidated:** no sweep has produced a reported number, and Gate 1 has never been measured. | D |
| D-093 | 2026-09-02 | **An outage window over which the vehicle does not move is dropped, counted and reported, exactly as a window with no truth coverage is (D-089).** `drift_percent` now raises `ZeroDistanceOutage`, a named subclass of `ValueError`, and `evaluate_sequence` drops that window; `summary.json` reports `dropped_windows` broken out by kind. Extends D-089's mechanism to a second cause rather than adding a second mechanism. | Found by running the sweep on the re-picked split: `S3c` has **one** window, 10 s at sample 36300, whose eleven truth epochs all carry the identical fix `(52.4074159, -1.4976300)` — a genuinely parked vehicle, not a loader defect. `drift_percent` refused to divide by a zero path length and the exception, being a bare `ValueError`, escaped `evaluate_sequence` and **killed the whole run after S3a had already produced 1191 results** — the same failure D-089 fixed, from a second cause it did not cover, so the enlarged split found it on the first sweep. `drift_percent`'s own message already said such a window "must be excluded from the sweep, not divided by", and again nothing implemented that. **The metric is undefined, not inconvenient:** drift-% is `‖p̂(T) − p(T)‖ / L`, and `L = 0` has no value to report — there is no threshold here to have moved. **The exclusion cannot flatter the result**, which is the thing worth checking before making it: a stationary window is the *easiest* case in the sweep, because ZUPT holds the filter almost exactly and the zero-order-hold GNSS baseline is exactly right too, so dropping it removes an easy window from the numerator *and* the denominator of a ratio of medians. It is dropped because the number does not exist, and would have been dropped even if keeping it helped. **A named exception rather than catching `ValueError`:** `evaluate_outage` reaches `crse`, `yaw_error` and `_validate`, all of which raise plain `ValueError` for real defects — a shape mismatch, an empty yaw sequence, an unhandled CRSE convention. Catching the base class in the per-window body would silently convert any of those into a dropped window and a slightly smaller result set, which is precisely the invisible failure D-067 exists to prevent. `ZeroDistanceOutage` is caught; nothing else is. **CTE, CRSE and yaw are discarded with it** even though they remain well-defined at zero distance, because a window contributing four metrics where its neighbours contribute five would make the per-method counts in `summary.json` disagree while every individual number stayed correct — the same atomicity argument as D-089, and the alternative is a summary that misreports without a wrong number in it. Nothing is invalidated: no sweep has yet produced a reported number. | D |
| D-094 | 2026-09-02 | **The filter is initialised from the `S-` stream's own GNSS and gravity before the first propagate, and `state.R` is set to `R_vehicle→NED @ R_sv` because `state.R` is phone→NED, not vehicle→NED.** `run_filter` gains `initialise_filter`: position from the first warmup fix, velocity and course from the warmup fixes differenced, `R_sv` from `pca_mount_yaw`, and `P₀` from `initial_covariance` carrying the PCA's measured `spread_rad` instead of its stated 5° fallback. Truth is not read. **This does not close Gate 1 and no ratio is reported.** | `run_filter` constructed `InEKF(cfg)` and propagated from `R = I`, `R_sv = I`, `v = 0`, `p = 0`; **`pca_mount_yaw` was called from `tests/test_mount.py` and nowhere else**, so D-073's initialiser and the `P₀` mount spread it exists to supply were built and never wired in. Measured before: max\|P\| after 600 s was **1.03e+12** on S3a, 1.22e+12 on Vta1a, 1.93e+11 on Vw2 — **identical with and without GNSS updates applied, to the digit**, which is what localised the fault to propagation-plus-initialisation rather than to the update family. After: **5.0e+04**, seven orders down, and the Vw4 sweep no longer dies in `np.linalg.inv`. **The frame composition is the part that is easy to get wrong twice.** `_h_vehicle_velocity` computes `v_veh = R_sv @ R.T @ v` and `propagate` applies `R` to the raw accelerometer, so `state.R` maps **phone**→NED and `R_sv` maps phone→vehicle; course over ground measures the *vehicle's* heading, so `R = R_vn @ R_sv` and not `R_vn`. Setting it to `R_vn` alone — which this change did first — leaves the mount out of the gravity cancellation. Verified by measurement rather than by argument: the mean residual specific force in NED over S3a's warmup is **0.105 m/s²** under the composed rotation, and all four candidate compositions were computed and compared before one was chosen. **What remains, stated because it is the live blocker and not a caveat.** The χ² gate still rejects **59 of 60** fixes on S3a, and the rejection sequence is now visible: fix 0 innovation **0.78 m** (accepted), fix 1 **120.40 m** after 9 s (accepted, χ² 9.3), fix 2 **825 m** (rejected, χ² 10312), and from there the state and the innovation run away together while `σ_p` grows too slowly to re-admit anything. 120 m in 9 s is ~3 m/s² of unmodelled acceleration, an order of magnitude more than the 0.105 m/s² levelling residual and 900× the 3.3 mm/s² accel-bias prior, so it is **not** bias and not levelling. That is a filter accuracy question inside Gate 1's own diagnosis order and it is seat S's, not this change's: the initialisation defect had to be removed first because it dominated everything behind it. Nothing is invalidated — no ratio has ever been reported. | D |
| D-095 | 2026-09-02 | **The 120 m is the gyro axis mapping: the `S-` column the harness integrates as the vertical body rate is not the vertical body rate, and `gyro_pitch` is. No code is changed and Gate 1 stays unmeasurable until a human resolves this against D-085.** `eval/axis_check.py` is added to measure it and `tests/test_axis_check.py` pins the estimator. `imu_stream` is left exactly as it is. | **Reproduced first.** On `S3a` at `5cae714` the χ² gate accepts fix 0 and then rejects; measured here, fix 1 innovation is **59.99 m** at χ² 253.8 (**rejected**), not the 120.40 m at χ² 9.3 (accepted) D-094 records — a disagreement reported rather than smoothed, and the qualitative failure is the same or worse. **The mechanism.** `‖v‖` runs 7.85 → **42.72 m/s in 9 s** → 535.99 m/s by 45 s. Decomposing `R @ accel + GRAVITY_NED` over S3a's first 60 s: with `R` **frozen** at the initialiser's value the residual is `[0.050, 0.002, -0.057]`, ‖mean‖ **0.075**, rms **1.11 m/s²** — a car's own acceleration. With `R` **propagated by the gyro** it is ‖mean‖ **12.77**, rms **12.90 m/s²**, and the attitude has wandered **87.0°** in 60 s. That is ~1.3 g of gravity tipped into the horizontal axes and integrated twice, and it is the "~3 m/s² of unmodelled acceleration" D-094 inferred — larger, and not bias and not levelling, exactly as D-094 argued. **Which column.** The vehicle's course over ground, differenced, regressed on each gyro column integrated over the same 30 s block: on S3a slope **−0.996**, R² **0.994**; S3c **−0.996**, R² **0.999**; S1 **−1.001**, R² **0.990** — a slope of −1 with no scale factor to fit, because both sides are the same angle in radians. `gyro_pitch` wins by R² on **18 of 18** stems the split can load. The column the code uses, `gyro_roll`, scores R² 0.071/0.682/0.651 on those three. Zeroing the horizontals and putting `gyro_pitch` in the vertical slot drops the residual rms from **12.90 → 1.11 m/s²** on S3a and from a median **6.27 → 2.43 m/s²** across 18 stems, and the S3a fix-1 innovation from **60.0 → 16.1–20.5 m** across the four proper candidates (15.3 m with the horizontals zeroed) with fix 5 from **10312 → 372–669 m**. **Why nothing is changed anyway, three reasons and any one is enough.** (1) **It contradicts D-085 on stems this container cannot reach.** D-085 resolved the gravity sign against `corr(heading rate, ω·ĝ) = −0.9860` on `S-T2` and `−0.9902` on `S-T7`; with `ĝ ≈ [0,0,−1]` that projection *is* `−gyro_roll`, so D-085 measured the vertical rate on the column this row measures as carrying almost none. T2 and T7 are unsynchronised-folder stems and unfetchable here (D-097), so the two measurements cannot be reconciled on this machine and one of them is wrong about something. (2) **The replacement is not fully measured.** Only the *vertical* component is: which of `gyro_yaw`/`gyro_roll` is device x versus y, and their signs, is invisible to every test tried — the shipped `GRAVITY` channel is pinned at `[0,0,9.8065]` with z-std 6e-5 and cannot see a horizontal rate, the `ORIENTATION` columns sit in gimbal lock at pitch −86.9° with azimuth and roll std 130°/111°, and the suspension response (roll rate against `d(a_lat)/dt`, pitch rate against `d(a_fwd)/dt`) returns |corr| ≤ 0.07. A right-handed triad narrows the eight signed permutations to **four** proper ones, and no evidence in this repo picks among them. (3) **The four are not interchangeable.** On S3a they span fix-1 innovation 16.1–20.5 m, which is immaterial; on **S3c they span 17.0–94.3 m against 14.4 m as shipped**, so on that stem the correction does not improve fix 1 at all — as shipped S3c even passes the gate there, χ² 0.4, and accepts 2 of 6 fixes against 1 of 6 for two of the candidates (it does improve fix 5, 344 → 183–270 m). S3c turns only 20.4° in its first 60 s, so the leak has barely built by fix 1 — consistent, but it means "the fix helps" is **not** yet true stem-by-stem. Picking the permutation by which innovation it produces is choosing the window that plots best, which is why it is not done here. **Nothing is invalidated and nothing is closed:** no ratio has ever been reported, Gate 1 is untouched, and this is item two of its fixed diagnosis order reached from the axis rather than the gate. Needs a human per G-8: it changes what the filter integrates, and it supersedes an axis inference D-047 made from labels and D-085 made from a projection. | D |
| D-096 | 2026-09-02 | **The `S-` column `GPS SPEED (Kmh)` is metres per second, and the loader's name `gps_speed_kmh` records a unit the file does not use.** Recorded, not renamed: the only consumer uses it for a sign. | Third header in this dataset to lie about its own contents, after `DATE (YYYY-MO-DD HH-MI-SS_SSS)` (D-086) and the gyro axis labels (D-095). Measured as `median(chord speed between consecutive fixes ÷ column)` over intervals above 2 m/s: **1.0039** on S3a (n=242), **1.0026** on S3c (n=376), **0.9991** on Vw2 (n=545), **1.0054** on Vw4 (n=1287). It would be 3.60 if the header were right. **Nothing is numerically wrong today**, which is why this is a row and not a fix: the sole consumer is `_forward_reference`, which divides by 3.6 and then uses the result only through the *sign* of a correlation, and a positive scale factor cannot change a sign. **It will be wrong the moment P-08 exists** — the speed head's target is this column, and a target scaled by 3.6 trains a head that is calibrated on nothing, which is the Gate 2 criterion. Renaming the allowlist entry touches `eval/loaders/columns.py`, which G-6 guards, so it is flagged for the owning seat rather than done in a diagnosis task. | D |
| D-097 | 2026-09-02 | **`eval/fetch.py` cannot fetch the unsynchronised folder at all: without `--sync-only` it raises `AssertionError` on the first row.** Recorded against the module committed at `492108c` this session. | `PREFIX = "IO-VNBD/"` and the assert is `rel.startswith(PREFIX)`, but all **276** unsynchronised manifest rows open `IO-VNBD-unsync/`, which starts with `IO-VNBD` and not with `IO-VNBD/`. The docstring already says the unsynchronised files are "what nothing in the protocol reads", so the gap is not urgent for the protocol — but it is exactly what blocked D-095 from re-measuring D-085 on `S-T2`/`S-T7`, and those two stems carry the only prior evidence about the gyro's vertical axis. The manifest also records their source as the git-lfs object `Unsynchronised V and S Dataset.zip`, so they are not individually addressable on `media.githubusercontent.com` the way the synchronised files are and the fix is not a one-character prefix change. Left for seat D's fetcher work; the consequence is stated here so the next session does not rediscover it. | D |

## Gate 1 — P-06: the Onyekpe INS reproduction, trained (2 Sep 2026)

| ID | Date | Decision | Reason | Owner |
|---|---|---|---|---|
| D-098 | 2026-09-02 | **The nine silences D-070 recorded are now filled in, and every value is ours:** 200-epoch cap with early stopping on validation MAE at patience 20 and the best weights restored; validation = whole TRAIN *families*, smallest first until 15% of windows; PyTorch default initialisation, seeded; no gradient clipping; constant 7e-4, no schedule; windows shuffled at stride = window (non-overlapping); and **GNSS input noise is not applicable** — the INS variant's input is six inertial channels, so there is no GNSS-derived input to inject noise into. `--audit` prints the table with each row's id. | Filling them in is the deliverable D-070 deferred, not a formality: half the reproduction is ours and the comparison has to be read that way. Two are worth their own sentence. **The validation rule took three attempts and the first two were bad in a way determinism does not fix** — IO-VNBD's families differ by two orders of magnitude in length, `S1` alone is 43% of the windows and `Vw14` 32%, so "every 4th family" left 2,461 windows to train on against 9,472 to validate, and "sorted until 20%" made a single drive the entire early-stopping signal. Smallest-first keeps both dominant drives in training and gives validation eleven families across three drivers. Each run prints the split sizes, so the next bad rule is visible before a model is fitted rather than after. **Gradient clipping is `None` and is written down as `None`** rather than omitted, so that the choice is visible in the same place as the eight that have values. | M |
| D-099 | 2026-09-02 | **The network regresses `(change in per-second travel, change in heading)` and integrates from the speed at outage entry — not per-second travel directly.** `--target distance` keeps the direct form, measured and reported below. Neither paper states what its INS network regresses; `models/baseline_rnn.py`'s "displacement-error correction" is the only guidance in the repo and it does not say error against what. | **A 1 s window of accelerometer and gyro does not contain absolute speed**, and the measurement is unambiguous: over the TRAIN stems per-epoch travel has σ = 5–8 m while the epoch-to-epoch *change* in it has σ = 0.67–0.77 m. An accelerometer integrates to a change in speed; asking a network for the speed itself asks for something not in its input, and it does the only thing it can — predicts the conditional mean. That is what happened: `--target distance` reaches a validation MAE of **3.54 m** against the **3.89 m** that predicting the training mean scores *without looking at the input at all*, and 53.7% median drift at 10 s. The same architecture on `delta_speed` reaches **0.275** and **11.6%**. Integrating from the entry speed is the concession D-064 already makes for the strapdown baseline: GNSS is available at outage entry, so a real system holds that speed. Recorded as a decision rather than a fix because it is a modelling choice the papers leave open, and because the failed form is the more instructive half — it is what an IMU-only network looks like when it has been asked for the impossible and has quietly obliged. | M |
| D-100 | 2026-09-02 | **The reproduction is trained and its number is reported as an improvement over raw INS, because that is the shape the published claim takes.** Median CRSE over the frozen sweep on held-out stems, against `eval/baselines.py`'s naive strapdown on the identical windows through the identical scorer: **10 s 11.40 m vs 57.07 m (80.0%), 30 s 72.78 vs 846.44 (91.4%), 60 s 214.26 vs 3,812.65 (94.4%), 120 s 542.82 vs 17,537.06 (96.9%), 180 s 952.59 vs 40,934.71 (97.7%)**. Median drift: 11.6 / 21.3 / 29.5 / 39.6 / 47.8%. Trained on 10,108 windows from 5 stems, early-stopped at epoch 22 of 43, 5,906 parameters. | The published INS result is stated as "up to **89.55%** improvement on INS displacement and 93.35% on orientation-rate", not as an absolute error table, so an improvement ratio against raw INS on our own windows is the only like-for-like comparison available — and ours brackets theirs at 80.0–97.7%. **Three caveats, all of which make the number smaller than it looks.** (1) The improvement percentage *flatters by construction*: raw INS diverges quadratically, reaching 40.9 km of CRSE at 180 s, so beating it by 97.7% is easier at 180 s than beating it by 80% at 10 s. The honest read is the drift column, and 47.8% at 180 s is nowhere near the 10% grade metric — which is the whole argument for the method this baseline exists to be beaten by. (2) **The 89.55% is transcribed from `compass_artifact_*.md`, a secondary source that this repo has already caught being wrong once** — P-01 found its CRSE gloss contradicted the paper's own Eq. (16) (D-054). It must be checked against Appl. Sci. 11(3):1270 before it reaches the write-up. (3) Three TRAIN stems (`M`, `S2`, `S4`) are excluded because they restart their clock mid-recording (D-013) and `assert_uniform_grid` refuses them; they are two of the four largest files, so the reproduction trains on roughly half the data the split nominally offers. | M |

---

## Sprint 1 — Gate 1 Blocker Resolution & Gate 0 Freeze (3 Sep 2026)

| ID | Date | Decision | Reason | Owner |
|---|---|---|---|---|
| D-101 | 2026-09-03 | **The gyroscope vertical axis is resolved as `gyro_pitch` with proper right-handed triad `(+gyro_yaw, -gyro_roll, +gyro_pitch)` in `eval/run.py::imu_stream`.** Supersedes D-047 and resolves D-095. `test_the_gyro_axis_mapping_is_the_one_d101_established` pins the mapping. | **Both sides of the conflict measured.** D-085 found correlation with `ω·ĝ` of -0.9860 on `S-T2` and -0.9902 on `S-T7`, where `gyro_roll` was the vertical axis; re-measured directly on `data/IO-VNBD-unsync/`, reproducing corr -0.9880 and -0.9910. However, `S-T2`/`S-T7` live exclusively in the unsynchronised folder. Across **17 of 17 loadable synchronised stems** (`S3a, S3c, Vta1a, Vta1b, Vta16, Vw2, Vw4, S1, Vw14a, Vw14b, Vw14c, Vta2, Vta4, Vta10, Vta14, Vw3, Vw11`), `eval/axis_check.py` measures `gyro_pitch` winning 17 of 17 on R² with unit slope (e.g. S3a slope -0.996, R² 0.994; S3c slope -0.996, R² 0.999). All evaluation protocol sequences are from the synchronised pool. **Triad selection.** Of the four proper right-handed triads, Candidate 1 `(+gyro_yaw, -gyro_roll, +gyro_pitch)` (spec `(0, 2, +1, -1)`) strictly dominates across stems: S3c fix-1 innovation is 17.0 m (chi² 0.6, accepted 2/6) vs 94.3 m for Cand 2, 28.3 m for Cand 3, 75.7 m for Cand 4; Vta1a fix-1 innovation is 5.7 m (chi² 1.3, accepted 6/6) vs 13.7–15.0 m for Cands 2 and 3; Vta1b fix-1 is 8.9 m (chi² 3.0, accepted 3/6); S3a fix-1 is 18.0 m (chi² 23.4). Zeroing the horizontals gave 88.4 m (chi² 552) on S3c and was rejected. **Before and after on S3a.** With `R` gyro-propagated under the shipped mapping, residual rms was 12.90 m/s², attitude wandered 87.0° in pitch, and ‖v‖ exploded 7.85 → 42.72 m/s in 9 s → 536 m/s by 45 s. Under Candidate 1, residual rms drops to 1.32 m/s² (against the 1.11 m/s² frozen-R baseline), attitude turns 87.0° around the vertical yaw axis (matching the true course change of 85.6° between fix 0 and fix 1), and ‖v‖ stays strictly within physical automobile bounds: min 0.01 m/s, max 30.27 m/s, ending at 6.22 m/s over 60 s. **Invalidates** any preliminary filter runs prior to this commit. | D |
| D-102 | 2026-09-03 | **The canonical GNSS speed column is renamed to `gps_speed_mps`, and `/ 3.6` in `_forward_reference` is removed.** Touches `eval/loaders/columns.py` (G-6), `eval/run.py`, `eval/axis_check.py`, `tests/test_leakage.py`, and `docs/EVALUATION.md` §1.1. Resolves D-096. | Re-measured `median(chord speed between consecutive fixes ÷ column)` over intervals > 2 m/s: S3a **0.9958** (n=240), S3c **0.9977** (n=367), Vw2 **1.0024** (n=542), Vw4 **1.0003** (n=1270). The values are metres per second, contradicting the `GPS SPEED (Kmh)` header. The division by 3.6 in `_forward_reference` is removed. Protects Seat M's speed-head training (P-08 / Gate 2) from a 3.6× calibration scale defect. | D |
| D-103 | 2026-09-03 | **`docs/EVALUATION.md` is frozen at Gate 0 status (03 Sep 2026).** Status flipped from DRAFT to FROZEN. Verified full agreement between `docs/EVALUATION.md` §3 and `eval/splits.py` across `LONG_OUTAGE`, `CHALLENGING`, `MANDATORY_PLOT_SEQUENCES`, and `UNAVAILABLE_S_STREAM`. Stale TODO in `eval/splits.py` replaced by record of D-092 re-pick. `tests/test_protocol.py` extended with `test_evaluation_doc_matches_splits_module`. | Formal Gate 0 closeout. Every held-out scenario group, long-outage member (7 stems, 459 60 s windows), mandatory plot sequence (S3a, Vw4), and unavailable stream exclusion in `docs/EVALUATION.md` §3 is now matched byte-for-byte with `eval/splits.py` and guarded by CI. Future protocol changes require a decision log row citing invalidated results. | D |

---

## Seat A — the foreground logger (3 Sep 2026)

> **Numbering note.** This section was written against D-089 while `main` still ended at D-084.
> `main` has since taken D-085 through D-100, and `d/gate1-sweep` holds D-101 through
> D-103, so these five entries were renumbered to D-104..D-108 when the merge landed. The log is
> append-only and resolved by highest number, so no branch may reuse an ID another has claimed.

| ID | Date | Decision | Reason | Owner |
|---|---|---|---|---|
| D-104 | 2026-09-03 | **The logger's main CSV is the harness allowlist exactly — 24 columns, in `Channels.HEADER`, and nothing else. Every other quantity the app records goes to a sidecar the harness never reads.** | Not a style choice: `io_vnbd.py::_canonicalise` runs `assert_no_leakage` over **every** column in the file, not over the ones it intends to use. The allowlist is therefore a whitelist of the whole header, and one well-meant extra column — `accel_uncal_x`, `battery_pct`, a temperature channel — raises `LeakageError` on the entire recording rather than being ignored. The cost of learning that empirically is a drive: the app cannot detect it, the failure appears on a laptop afterwards, and by then the road conditions are gone. `tests/test_android_logger_schema.py` parses the header out of the Kotlin source and runs the real guard over it, so the contract breaks in CI instead. | A |
| D-105 | 2026-09-03 | **Both calibrated and uncalibrated inertial streams are recorded. Calibrated goes in the main CSV; uncalibrated goes to the sidecar, with the OS bias estimate that arrived alongside it.** | `android/README.md` says to use `TYPE_ACCELEROMETER_UNCALIBRATED` / `TYPE_GYROSCOPE_UNCALIBRATED`, because the calibrated types subtract an OS bias estimate the filter is also estimating and the two then fight over one state. That is correct **for what the filter consumes**. But D-085 established what IO-VNBD contains: AndroSensor's `TYPE_ACCELEROMETER` plus a separate `TYPE_GRAVITY`, calibrated, gravity included — not a raw IMU log. A file whose `accel_*` columns held uncalibrated values would load through the same loader and silently mean a *different physical quantity* than every sequence it gets compared against, which is the kind of error that survives review because nothing about it looks wrong. Recording both costs one extra sensor registration and settles the question by having the data rather than by choosing. The uncalibrated types report the estimate in `values[3..5]`; keeping it is what makes the pair reversible in either direction after the fact. | A |
| D-106 | 2026-09-03 | **The main CSV is written at 10 Hz. The raw sidecar is written at the requested rate, default 100 Hz.** | `SAMPLE_RATE_HZ = 10` is a module constant in `eval/loaders/io_vnbd.py` and `eval/outages/inject.py` sizes every outage window from it, so a 100 Hz file would load cleanly and then be windowed as though it were ten times longer — a 60 s outage over 6 s of road, reported as though it were 60. The failure is arithmetic, not an exception, so nothing would raise. The other half of the trade is why the sidecar exists at all: 10 Hz cannot reach the short-τ end of an Allan run, and the sidecar is the only place a variance run on *our own* hardware can come from. 100 Hz rather than 200+ by default because above 200 Hz the API 31 cap applies unless `HIGH_SAMPLING_RATE_SENSORS` takes effect, and sustained high-rate sensing plus three writer threads heats the SoC until it throttles — which presents as a falling achieved rate partway through a drive, i.e. as the exact measurement this app exists to make, ruined. | A |
| D-107 | 2026-09-03 | **The `date` column's sub-second separator is a dot (`2026-09-03 14:05:07.123`), not the colon IO-VNBD ships, and the values are not wrapped in quotes.** | Three spellings are in play and they do not all parse in the same places. The IO-VNBD header advertises `HH-MI-SS_SSS`; its bytes contain `HH:MM:SS:mmm` wrapped in literal single quotes; `truth.py::_S_DATE` accepts that shipped form only after D-086, which is on an unmerged branch. Measured against both patterns: the dot parses **before and after** D-086, the colon parses only after. So a recording made today stays readable whichever order the branches land in, and the app does not acquire a dependency on an unmerged fix. The wrapping quotes are a defect in a file we are not writing; `_S_DATE` tolerates them post-D-086, but copying a bug forward to look authentic is how it becomes permanent. Asserted in `tests/test_android_logger_schema.py` against the live loader pattern, so if D-086 lands and changes the shape, the test says so. | A |
| D-108 | 2026-09-03 | **The recording service is `START_NOT_STICKY`, and the CSV row clock comes from the accelerometer's own timestamp rather than from the 10 Hz tick.** | Two separate ways a recording can lie about time, both closed the same way. **Not sticky:** if the system kills the service mid-drive, restarting it would open a second session file with a gap in front of it that is invisible in the data — the file would look complete and be missing a tunnel. A recording that stops is one you know about. **Row clock from the sensor:** the tick is a scheduler artefact and drifts under load; using it would be exactly the nominal-Δt substitution D-013 forbids, and the drift would be indistinguishable from sensor jitter in the very measurement the app exists to make. A tick that arrives late therefore costs a sample, never a wrong timestamp, and rows before the first accelerometer event are not written at all. | A |

---

## Rebase — the logger's speed column against D-102 (4 Sep 2026)

| ID | Date | Decision | Reason | Owner |
|---|---|---|---|---|
| D-109 | 2026-09-04 | **The Android logger writes `Location.getSpeed()` unscaled, the main-CSV header for it reads `GPS Speed (m/s)`, and `Channels.kmh` becomes `Channels.mps`.** Touches `Channels.kt`, `GnssHub.kt`, `Records.kt`, `RecordsTest.kt`, `android/README.md` and `tests/test_android_logger_schema.py`. | Found by rebasing `d/gate1-sweep` onto the merged `main`: nothing conflicted textually and the schema test failed anyway. D-104 pins the logger's header to what the harness allowlist normalises, and `columns.py` maps `^gps speed` to `gps_speed_mps` — so the app's `GPS Speed (Kmh)` column was being read as metres per second while `Channels.kmh` multiplied it by 3.6 on the way in. The `/ 3.6` that used to cancel it lived in `_forward_reference` and D-102 removed it, which means every recording this app made would have entered the harness 3.6x fast, in the one column D-102 exists to protect (P-08's speed-head target — a target scaled by 3.6 trains a head calibrated on nothing). The header label changes rather than staying byte-identical to IO-VNBD because D-107 already settled that question in the other direction: the app does not reproduce a defect in a file it is not writing, and `(Kmh)` on a column of metres per second is that defect. | D |

---

## Gate 1 — measured for the first time, and it fails (6 Sep 2026)

| ID | Date | Decision | Reason | Owner |
|---|---|---|---|---|
| D-110 | 2026-09-06 | **`run_filter` raises `FilterDivergedError` -- caught in `evaluate_sequence`, re-raised as `SequenceUnusable` -- when `|v|` leaves physical bounds. Touches only `eval/run.py`. With that guard, the sweep ran to completion for the first time in this project's history, and **Gate 1 is measured and fails**: at 60 s, median filter drift 136.8% against a GNSS-available baseline of 7.0% -- a ratio of **19.4x** against the required **3-5x**, resting on **3 windows**, with 6 of 12 held-out stems (`S3a, S3c, Vta16, Vta1a, Vw2, Vw4`) skipped for divergence between 266 s and 785 s. **The cause is localised and it is not the harness, the axis mapping, or the update algebra.** It is (1) the NHC pseudo-measurement, applied ungated against a mount rotation that is 2-37 deg uncertain and has zero process noise, and (2) a process-noise model built from a static Allan run that under-predicts real error growth by about 100x. Both are deferred work (D-005, D-048) coming due, not a regression. The fix is seat S's; this row measures it and stops.** | **What was excluded first, because a diagnosis that skips this step is a guess.** *The update algebra is correct*: at every fix on `Vta1a` the posterior innovation divided by the prior matches the Kalman prediction `R(HPH^T+R)^-1` to three decimal places -- 0.302 vs 0.293, 0.381 vs 0.382, 0.171 vs 0.172, and so on for sixteen consecutive fixes -- so `_apply_update`, the section 7.4 Jacobian and D-050's sign are all doing exactly what they claim. *The frames are correct*: filter velocity against the GNSS chord over `Vta1a`'s 1 Hz fixes agrees to within 1 m/s in speed and a few degrees in heading once aided, which a wrong axis or a wrong mount sign could not produce. *The harness is correct*: `align_to_sequence`, `imu_stream`, `fix_arrays` and `initialise_filter` were each checked in isolation on `Vw4` and each behaves as documented. **The measured mechanism.** The filter is grossly inconsistent -- median normalised innovation squared per degree of freedom is **2320 on Vta1a, 4691 on Vw4, 12538 on S3a**, where a consistent filter sits at 1. It believes it knows its position to ~4 m after 9 s of unaided propagation; the measured error over the same span is **200-370 m**. So every fix blows the chi-squared gate, nothing is applied, the state is unaided, and it runs away -- 1 of 60 fixes accepted on S3a, 1 of 61 on Vw4, 20 of 537 on Vta1a. D-094 saw the first ten seconds of this on one stem; this is the whole split. **NHC is the dominant term, and it is the one update that is never gated.** `update_gnss` gates and `update_zaru` gates; `update_nhc` calls `_apply_update` directly, thousands of times per run (6156 on S3a), asserting zero lateral and vertical *vehicle-frame* velocity through an `R_sv` whose 1-sigma at initialisation is **15.6 deg on S3a, 36.8 deg on Vta1a, 2.0 deg on Vw6** and whose process noise `mount_rw` is **exactly zero** (D-048), so that uncertainty can only ever shrink. Asserting a constraint through a rotation wrong by tens of degrees puts a component of the *forward* velocity into the lateral row and drives it to zero at sigma = 0.5 m/s -- `nhc_is_valid`'s own docstring calls this "a confident wrong measurement ... strictly worse than applying nothing", and nothing checks for it. Measured three ways. *Switch it off*: GNSS acceptance on S3a goes 1.3% -> 15.0%, on Vta1a 8.3% -> 21.5%, on Vw6 6.7% -> 28.6%. *Switch it off and size Q correctly*: S3a reaches **97.0%** acceptance and Vta1a **100%** at 2.6 m median innovation -- a filter that simply works. *Gate it instead of removing it*: **78.3% of NHC applications are refused** at a 2-dof 99% gate, which is the direct statement that the constraint does not fit the data it is being applied to. **Why none of that is the fix, and why it is not applied here.** NHC is also load-bearing: during an injected outage it and ZUPT are the only constraints there are, so both removing it and gating it make the *sweep* worse (10 of 12 stems skipped either way) even as they make the aided filter better. The real repair is the modelling both D-005 and D-048 deferred -- a non-zero `mount_rw`, an `r_nhc` that reflects the mount error instead of a flat 0.5 m/s, and a process noise that represents dynamic error rather than a stationary phone's noise floor -- and choosing those values is a filter-accuracy decision with `core/reference/inekf.py`'s owner, not one to guess at two days before a screening. Scaling process noise 100x uniformly was tried end to end and is **not** a fix: it makes the sweep worse (7 stems skipped, ratio 27.0), because inflating P also raises the gain on the ungated NHC. **A third fact, independent of any of this, and it constrains what Gate 1 can ever show.** Ten of the twelve held-out stems carry GNSS at a **9 s** cadence, and 9 s of unaided propagation on this IMU costs 200-370 m -- consistent with D-100's independently measured strapdown baseline. On those stems the "GNSS-aided" filter is barely aided *outside* the injected outages either, so it enters every window already carrying hundreds of metres. The two 1 Hz stems (`Vta1a`, `Vta1b`) are the only ones where an aided filter is really being tested, and D-092 deliberately re-picked the split *away* from that minority for representativeness. That trade was made without this measurement in hand and should be re-read with it. **What is safe to conclude:** no result in the repo is invalidated, because no ratio was ever reported; Gate 1 does not close on a configuration change; and the guard added here is a safety net, not a tuning knob -- it converts an unnamed `OverflowError` raised inside `van_loan`'s `expm_series` several hundred steps downstream into a message naming the sample, and marks the whole stem unusable rather than one window, because a continuous pass that left physical bounds has corrupted every window sliced from it (D-089's atomicity argument). The numbers above come from a dirty tree; a clean re-run should be the one cited anywhere they are quoted. | D finds, S owns the fix |

---

## Seat A — the second app, and the rules that had no test (10 Sep 2026)

| ID | Date | Decision | Reason | Owner |
|---|---|---|---|---|
| D-111 | 2026-09-10 | **`android-ui/` makes no network call at runtime, and the `INTERNET` permission is removed from its manifest so that a future one fails rather than works.** The CartoDB tile source behind its `osmdroid` map, the `ui-text-google-fonts` Inter family, the `com.google.android.geo.API_KEY` placeholder and the `INTERNET` / `ACCESS_NETWORK_STATE` permissions are all gone. The map becomes a Compose canvas over the recorded track in metres, with a 50 m graticule, a scale bar and a caption saying there is no basemap and the line is not matched to a road. | D-041 claims the system is 100% offline and D-080 forbids the demo surface any fetch; `android/HANDOVER.md` §2 constraint 3 spells out that this rules out every tile server and every CDN font. `android-ui` arrived on 5 Sep (`9a6f6c2`) with three of them at once, and none is visible at the call site -- one is a `MapView`, one is a `FontFamily`, and the third is a manifest line. **The failure they buy is not "the map is blank".** Rehearsals happen on a phone that has the tiles cached and the font resident, so the demo passes every time it is practised and fails once, at a venue, in front of judges, with no fallback. Removing the permission rather than only the call site is the part that lasts: the claim is now checkable by reading the manifest, and a reintroduced fetch dies at runtime on a developer's own device instead of working there and nowhere else. Three secondary reasons the tiles were wrong even with a network: road geometry under a track reads as a **match**, and there is no matcher until seat P's October work (HANDOVER §3b); the tiles were anchored at a hard-coded Delhi origin, so a recording made anywhere else was drawn over streets it was never driven on; and `osmdroid` writes a tile cache, which is state a recording device does not need. | A |
| D-112 | 2026-09-10 | **No Android surface displays a value it was not given. `PREVIEW_TELEMETRY` is deleted, and with no recording the operator UI shows that it has no data; the middle metric card is labelled `m est. σ` rather than `m drift`; `LocalNavigationEstimator` carries a header naming the three things it is not.** | Two separate rules, broken in the same file. **The invented data (D-080):** a `PREVIEW_TELEMETRY` constant was rendered whenever the service was not running -- on launch, in Android Studio, and on any phone without permissions -- carrying a 13.8 m/s speed, a fourteen-point track, `sampleRateHz = 200f` and `timestampJitterMs = 0.8f`. Those last two are per-device numbers `android/HANDOVER.md` §1 says have never been measured on any phone in this project, and 200 Hz is the FOG configuration D-081 requires captioned as *not demonstrated*. An unmeasured figure rendered in the same typeface as a measured one is exactly the cockpit-fed-by-its-own-generator failure D-039 and D-080 exist to prevent, and a screenshot of it is indistinguishable from evidence. It stood for five days. **The mislabelled metric (D-079):** the card read `m drift` over `uncertaintyM`, which is the demo estimator's own uncertainty -- grown by a hand-chosen `0.08*sqrt(dt)` while GNSS is stale, not propagated. Drift is error against truth as a percentage of distance travelled and it is the metric PS 26168 is graded on; a phone has no truth, so this app cannot produce it. Putting the graded metric's name over a number that was never compared to truth is the same class of error as a renderer recomputing one. | A |
| D-113 | 2026-09-10 | **Every §2 constraint that can be checked in text now has a test in `tests/test_android_demo_surface.py`, in the Python suite, over both Gradle roots -- and the Kotlin guard in `android/app/src/test` stays as the stronger check for when a toolchain exists.** | Both violations above reached `main` through commit messages describing something else: "Add updated Android UI frontend" for the app, "chore: Update execution history and build output cleanup lock files" for the 700-line replay view. Neither was read against the constraint list, because a constraint list that lives only in a document is one that gets bypassed by anyone who has not read that document -- which is every new contributor, by definition. The choice of **Python** for a test about **Kotlin** is deliberate and is the whole point: `TrajectoryRecordTest` makes several of these assertions already and is the better test, and until D-114 it ran nowhere, because the Android SDK is a 400 MB download from `dl.google.com` that no contributor's laptop is guaranteed to have and that this project's own remote environment blocks outright. A weaker check that runs on every commit beats a stronger one that runs on no machine. Text matching cannot see a violation expressed a way the regex does not know, so it is not a substitute for review -- but the guard is not merely *observed to pass*: ten reintroduced violations (the tile source, the font dependency, the `INTERNET` permission, the preview constant, a literal sample rate, a dropped caption, an RNG import, a division by `distanceM`, a renamed harness field and a schema-string drift) were each checked to make it fail, the way the leakage audit verifies its own guard in `ci.yml`. | A |
| D-114 | 2026-09-10 | **CI compiles both Android roots: `.github/workflows/android.yml` runs `:app:assembleDebug` and `:app:test` for `android/` and `:app:assembleDebug` for `android-ui/`, in a workflow separate from `ci.yml`.** | HANDOVER §4 item 8, unblocked since `53e0053` committed the wrapper and not done until now: 4,300 lines of Kotlin across two modules had never been compiled by anything but a laptop, while the Python suite stayed green and made the repo look healthy. Separate from `ci.yml` so that the critical-path harness checks stay a two-minute job that cannot be blocked behind an SDK download. It earned its place before it ever ran: `android/gradlew` was committed **non-executable** (mode 100644, where `android-ui/gradlew` is 100755), so `./gradlew` on any Linux runner would have stopped at "permission denied" -- a defect invisible for as long as the only machines running it were a Windows box and an IDE, and one that would have hit the first contributor on Linux. Fixed with `git update-index --chmod=+x`. | A |

## Gate 1 — the fix that holds the aided pass, and the split the data forces (13 Sep 2026)

| ID | Date | Decision | Reason | Owner |
|---|---|---|---|---|
| D-115 | 2026-09-13 | **The filter is aided by the receiver's Doppler velocity at every open fix, its process noise is read from the stream it is running on, its prior is re-expressed in the right-invariant coordinates it actually carries, the stop detector is tightened to what the truth track says a stop looks like, and a position fix rejected three times running is applied. Touches `core/reference/inekf.py` (`update_gnss_velocity`, `right_invariant_from_plain`, `update_gnss(gate=)`, `FilterConfig`: `gnss_speed_sigma_mps`, `gate_gnss_velocity`, `chi2_gate_2dof`, `mount_rw`, `gyro_bias_turn_on`, `course_*`, `zupt_*`), `eval/run.py` (`velocity_measurement`, `in_motion_config`, `stream_white_level`, `GNSS_REJECTIONS_BEFORE_REANCHOR`, the in-motion alignment and per-window snapshot/replay that the 12 Sep session built), and the tests that pinned D-045's bias instability in `P0`, D-048's zero `mount_rw`, and the 0.5 s / 0.05 / 0.02 detector. **Gate 1 re-measured on a dirty tree, scratch output not committed: at 60 s, median filter drift 78.6% against 6.7% GNSS-available, ratio 11.8x on 205 windows (D-110: 19.4x on 3), with `Vw4` and `Vta16` skipped for divergence of the aided pass and 81 windows dropped for divergence in replay. Split by what the recording can support: S3a and S3c, 56.7% / 9.1% = 6.2x on 101 windows (S3a alone 44.3% / 8.2% = 5.4x); the ten Vta/Vw stems 86.8% / 3.9% = 22x. At 10 s the S3 pair is 41.4% / 21.2% = 1.95x. Gate 1 does not close, and the row below says which part of the remaining gap is the recording, which is the filter, and what the next fix is.** | **What was measured first, against the paired `V-` track at 10 Hz, on every held-out and TRAIN stem that loads (`scratchpad` of the 13 Sep session).** *(1) The dataset is two populations.* The per-sample white level of the 10 Hz gyro while moving -- residual from a 0.5 s centred mean -- is 1.7-1.9 deg/s on S3a and 0.6-1.5 on S3c, against 4-20 deg/s on every Vta/Vw stem (Vta1a 5.9 / 12.4 / 18.9 by axis, Vw4 6.8 / 9.5 / 11.9); the accelerometer 0.4-0.5 m/s^2 against 1.4-3.5. Integrated about the vertical and compared with the truth course over windows of 10 / 30 / 60 / 120 s, with a per-stem constant bias removed, the heading error is 1.8 / 2.3 / 2.9 / 4.1 deg median on S3a and 1.5 / 2.2 / 2.9 / 4.9 on S3c, against 8.6 / 15 / 21 / 38 on Vta1a, 16 / 29 / 44 / 67 on Vw4, 12 / 21 / 28 / 42 on Vw2, 10 / 19 / 38 / 54 on Vta16 -- a random walk, not a bias. The gyro yaw rate against the truth course rate correlates at 0.95 raw and 0.99 with 4 s smoothing on S3a; on Vta1a 0.22 raw and 0.86 at 4 s with a regression slope of 0.05 rising to 0.69, on Vw4 0.27 and 0.70. Ninety-five percent of the yaw-axis variance on those recordings is not vehicle yaw; it is vibration folded into 0-5 Hz by sampling at 10 Hz without an anti-alias filter, and a bounded rattle aliased broadband random-walks the integral. **No filter recovers a heading from that stream: a 20-45 deg heading error at 60 s is by itself 17-30% of distance, sideways, before any speed error.** The `V-` stems are 10 of the 12 held-out stems and 18 of 19 TRAIN. *(2) The tilt random-walks in motion at 20x the Allan figure.* Integrating the 3D gyro from an accelerometer-levelled start and comparing with a levelling at the end, only where the truth shows both ends dynamically quiet, S3a's tilt error is 1.1 / 2.3 / 3.7 / 7.0 deg at 10 / 30 / 60 / 120 s -- about 0.5 deg/sqrt(s), where `gyro_arw` = 1.41 deg/sqrt(hr) is 0.024 deg/sqrt(s). That is D-110's "process noise under-predicts by about 100x", measured: 20x in sigma. `in_motion_config` now sets `gyro_arw` and `accel_vrw` per stem from the warmup's own white level (x22 and x22 on S3a, x43 / x25 on S3c, x195 / x122 on Vw4), floored at the Allan values, which overstates the walk by 1.4-2x where the energy is bounded rattling -- D-045's conservative direction. *(3) The receiver's Doppler is a good velocity.* `gps_speed_mps` against the truth speed at the fix epochs, eight stems: unbiased, RMS 0.27-0.92 m/s; `gps_orientation_deg` against the truth course: RMS 1.2-6.0 deg, i.e. 0.22-0.48 m/s across track. `gnss_speed_sigma_mps` = 0.5 and the 12 Sep session's `course_cross_track_sigma_mps` = 0.5 are those numbers. *(4) The stop detector fires at speed.* At 0.5 s / 0.05 / 0.02 rad/s, 31% of its firings on S3a and 41% on S3c are while rolling, median 4.1 and 7.7 m/s, p90 13.8 and 20.4 m/s: a quiet phone at motorway speed satisfies an IMU-only detector. At 2 s / 0.02 / 0.01 that is 0% and 1.9% (median 1.5 m/s, the run-in to a stop), at 76% recall of S3c's stops. S3a's stops are not detectable at either setting: its gyro norm at a standstill is 1.55 deg/s median, which is its own bias, and the detector reads the raw norm. `tests/test_allan.py` and `tests/test_filter.py` carry the new thresholds; `eval/allan.py` selects segments by the same rule and its committed artefacts were made under the old one, which is noted here and not regenerated. *(5) The receiver's accuracy is understated.* Fix error against truth over reported `gps_accuracy_m`: median 1.36 on S3a, 1.71 on S3c with 21% of fixes beyond 3 sigma and a p99 of 10 sigma. A 99% gate on that `R` refuses a fifth of S3c's good fixes. **What was wrong in the filter, in the order it was found on S3a with every fix open.** Under the 12 Sep tree the aided pass left physical bounds at 59 s. The first fix at 9 s carried 13 m of innovation because the speed had fallen 3.4 m/s behind an accelerating car -- a 2-3 deg levelling error -- and the filter, whose 9 s velocity uncertainty is dominated by the 0.2 deg/s gyro-bias block, explained it as 0.25 deg/s of bias; the wrong bias tilted the attitude 2 deg more by the next fix, whose 52 m innovation the gate refused, and nothing was applied again (`b_g` 0.5 deg/s, tilt 14 deg, 209 m at 27 s). Switching NHC or ZUPT off, scaling Q by 20, or both, moved the divergence to 90-250 s and did not prevent it: a position every 9 s is the double integral of tilt, bias and heading errors together and cannot separate them. **The change that made the pass hold is the Doppler velocity update, ungated.** With it the same run holds 0.8-11 m for 600 s, corrects a 6 deg tilt error to 1.3 deg at the first fix that sees it, and keeps speed within 1 m/s and heading within 3 deg of truth throughout. Gated at 99% (2 dof, 9.21) the second Doppler fix is refused (NIS 6.5 per dof on a 6.35 m/s innovation the tilt block did not cover) and the lock-out is permanent -- position error 15.0 m median / 144 m p90 over the whole of S3a, 11.3 / 144 at 99.99%, 10.6 / 58 ungated; Vta1a 15.6 / 46, 14.7 / 43, 10.2 / 16; Vw2 diverges under either gate and holds 57 / 207 without. D-057's argument for not gating ZUPT applies verbatim. **The position gate then locks out on its own**: with velocity re-pinned every fix, `P_pp` grows by (0.5 m/s x 9 s)^2 per interval and a 200 m offset made during one tilt excursion is never re-admitted; S3a spent 100 of 254 fixes locked out with a 3 m fix in hand. Three consecutive rejections -- a 1e-6 event under the filter's own model -- now apply the third fix and are counted as `n_gnss_reanchored` (S3a 22, S3c 48, Vta1a 46, Vw2 139 over their recordings). **A diagonal `P0` in right-invariant coordinates was never a diagonal prior**: `xi_p = dp + p^ dtheta`, so the constructor's `P0` at 200 m from the origin claims a 50 m position uncertainty attributable to the 14.6 deg yaw block, and a forced fix there *rotates* the state 80 deg about the origin instead of translating it (`test_the_caller_can_override_the_position_gate` demonstrates both). `right_invariant_from_plain` applies the section-5 map once, at alignment, so `H P H^T` for a fix is the plain position block and the height-to-tilt correlation `|p| sigma_tilt` that the 12 Sep session attributed to `GNSS_VERTICAL_SIGMA_RATIO` is gone at its source. **Why the 60 s windows are still at 44% on S3a, and what it is not.** Replaying S3a's forty 60 s windows from their aided snapshots: heading error over the window is 1-5 deg median in the first two thirds of the drive and the speed error at 60 s is -3 to -17 m/s -- the speed collapses. The aided pass shows why. Its gyro-bias estimate walks to 0.3-0.5 deg/s on the horizontal axes in the last ten minutes of S3a (whole-file mean 0.01) and to 0.65 / 1.1 m/s^2 of *accelerometer* bias on S1, 15-25 sigma of its own process model; between fixes that bias tilts the attitude 3-4 deg, the velocity runs 5-10 m/s off, and at a stop-and-go the filter drives backwards until the next Doppler fix snaps it (S3a, 2007-2013 s and 2034-2039 s). Every window opens from that state. The root is that the bias is never observed directly: ZARU never runs on S3a because the raw gyro norm at rest is the bias itself (1.55 deg/s against a 0.57 deg/s threshold), and at a 9 s cadence tilt, gyro bias and accelerometer bias are separable only through turns. Scaling `accel_bias_rw` by 0.01 or 0.1, `gyro_bias_rw` by 0.1, or the turn-on prior to 0.05 deg/s moves the 60 s median by under 1%. **Next, in order, and none of it is done here**: a stop detector that reads the bias-corrected gyro and the accelerometer variance alone (S3a stopped: variance 0.0075 median against 0.09 rolling p10 -- a 12x separation the current rule throws away), so ZARU runs; a turn-on prior for the accelerometer bias, measured, in place of the 0.34 mg instability; and the plan's bias snapshot at outage entry. **What is safe to conclude.** On the two recordings whose IMU stream can support dead reckoning at all, the physics-only filter is 5-7x the GNSS-available baseline at 60 s and 2x at 10 s, limited by bias observability at a 9 s aiding cadence, which is a filter problem with a named next step. On the other ten the stream itself puts heading beyond 20 deg at 60 s and no filter closes that; the honest statement for the write-up is that IO-VNBD's 10 Hz phone stream on those recordings is not an IMU for this purpose, which is exactly why D-106 records our own logger at 100 Hz. The Gate 1 ratio should be quoted with that split, never pooled. Nothing here touches `docs/EVALUATION.md`; the Doppler velocity is the same receiver at the same availability as the position the protocol already admits, and it is never called inside an outage. The NEES tests: with the turn-on bias in `P0`, ZARU-only measures 16.63 against [16.84, 19.20], 1.3% under-confident, and asserts one-sided as the full-state test already does; the other four are unchanged in band. `test_zupt_does_not_shrink_the_mount_covariance` compares against a propagate-only twin now that the mount block grows. The numbers above are from a dirty tree and a scratch output directory; the committed `eval/figures/summary.json` is still D-110's and a clean re-run is the one to cite. | S (filter), D (harness, measurement) |

---

## Demo Operator UI — Live Online OpenStreetMap Integration (13 Sep 2026)

| ID | Date | Decision | Reason | Owner |
|---|---|---|---|---|
| D-117 | 2026-09-13 | **The interactive demo operator UI (`android-ui/`) adopts online map tile streaming (OpenStreetMap via OSMDroid) with pinch-to-zoom and pan, while the submission evaluation logger (`android/`) remains strictly 100% offline (D-041, D-104-D-109). Stationary uncertainty error growth in `LocalNavigationEstimator` is stabilized via Zero Velocity Updates (ZUPT).** | The user directed live online map cartography and zoom controls for real demo evaluation. The evaluation logger (`android/`) retains its strict 100% offline guarantee without network permissions for hackathon dataset collection and benchmark reproducibility. In `android-ui/`, the runaway indoor uncertainty drift (caused by continuous $0.12\sqrt{\Delta t}$ growth when GPS was unavailable while sitting at a desk) is eliminated: when stationary (`isStationary || speedMps < 0.15f`), uncertainty covariance does not expand; during motion in GNSS denial, uncertainty grows physically with distance traveled and calibrated gyro drift. | A |
| D-118 | 2026-09-13 | **`LocalNavigationEstimator` adopts discrete step-driven PDR displacement and ground-plane forward axis projection ($\text{atan2}(R_{01}, R_{11})$); suppresses coarse network provider fixes when GPS is active; enforces instantaneous velocity stop on cadence timeout.** | Observed during real walking tests on held device: pacing back and forth on a single track caused drifting looping tracks and a runaway straight vector upon stopping. Root causes: (1) Raw Euler angles from `SensorManager.getOrientation` assume horizontal device; held at 45° in portrait mode, body roll and wrist tilt cross-contaminate azimuth, warping turns into asymmetrical loops. Fixed by projecting device forward axis $[0, 1, 0]^T$ from the 3D rotation matrix directly onto the world ground plane, yielding pure tilt-immune horizontal bearing. (2) Continuous velocity integration without discrete step boundaries allowed speed to persist indefinitely when stopping, cruising across the map. Fixed by making PDR strictly step-driven: each step queues $\Delta d = \text{stepLen}$, consumed smoothly along the step azimuth; if no step occurs within 1.1s, velocity drops to 0.0 km/h and position is firmly locked (ZUPT). Turns in place rotate the pointer without adding lateral translation, allowing back-and-forth movement to cleanly retrace the exact same path. | A |
## Seat A — first physical device measurement, and the retirement of "not measured" (13 Sep 2026)

| ID | Date | Decision | Reason | Owner |
|---|---|---|---|---|
| D-116 | 2026-09-13 | **The foreground logger is installed and measured on a physical team device (Samsung Galaxy A55 5G, `SM-A556E`, Android API 36) over a 246.6 s stationary desk session (`S-IDR-20260913-031148-samsung-sm-a556e`). Achieved sample rate is 125.0 Hz across all streams against 100 Hz requested; median and p95/p99 Δt jitter is 8.1 ms (stdev 0.0030 ms); clock timebase is verified as `ELAPSED_REALTIME` with 0 non-monotonic stamps and 0 gaps; sensor parts are STMicroelectronics LSM6DSVTR (accel/gyro) and Haechitech MXG4300S (mag); zero warnings. Sidecar committed to `android/measured/`. The "not measured" assumption in `android/README.md` and `android/HANDOVER.md` is retired for this device.** | First physical hardware verification of Seat A's logger. D-106 sized the logger at 100 Hz on the assumption that an Android device delivers it; this run proves the HAL delivers a continuous 125.0 Hz (min delay 2,000 µs on the STM LSM6DSVTR IMU) without OS rate-limiting or mic-access degradation. Monotonicity and zero dropped samples confirm the foreground service and lock-free ring buffer architecture hold up under continuous 125 Hz write load (10.8 MB raw IMU CSV over 4 minutes). The 10 Hz main CSV loads directly through `eval.loaders.io_vnbd::load_sequence` with 2,462 canonical rows and zero leakage errors, verifying that real on-device data matches the frozen harness protocol. | A |
| D-119 | 2026-09-13 | **The Allan run on our own hardware (android/HANDOVER.md §9 item 7) is executed and its figures are recorded next to IO-VNBD's, not in place of them: `FilterConfig` is unchanged.** A 1466.7 s stationary desk session on the team Galaxy A55 5G (`S-IDR-20260913-141119-samsung-sm-a556e`, `status: complete`, zero warnings, zero dropped rows, 549,987 raw rows) is read at its own 125.03 Hz by `eval/allan.py` through the new `eval/loaders/android_raw.py`, which pairs accelerometer and gyroscope on the sensor's `event_ns` (183,329 pairs, 0 unpaired), gates stationarity on the OS-bias-compensated gyro and computes the curve on the raw one. The segment finder returns three stationary runs (554.6 s, 367.2 s, 195.2 s) and **none passes D-045's quiet margin**: accelerometer variance 0.0005–0.0011 (m/s²)² is inside the 0.002 margin, mean \|ω\| 0.0026–0.0031 rad/s is not (margin 0.001). Computed under `--include-noisy` and written to `eval/figures/device/S-IDR-20260913-141119-samsung-sm-a556e/`: **gyro ARW 0.56–1.19 °/√hr** on the axes whose fit band is white (worst 1.19, `gyro_roll`, slope −0.44), **accel VRW 0.07–0.15 m/s/√hr** (`accel_z` white on all three segments), **gyro bias instability 18.6 °/hr** (τ 13–56 s, at the record's edge on most axes), accel bias instability 0.33 mg. Against IO-VNBD's phone (D-045): ARW 1.41 °/√hr, VRW 0.45 m/s/√hr, B 42 °/hr, 0.34 mg. | Four things. (1) **Why the gate says no, and why it is left alone.** The quiet margin is a bound on mean \|ω\| per sample; per-sample gyro noise scales with the square root of the bandwidth, and at a 125 Hz ODR (≈62.5 Hz band) a white ARW of 0.0002–0.0003 rad/s/√Hz alone puts the roll axis at σ ≈ 0.002 rad/s, so the margin cannot be met at full rate on sensor noise alone — the IO-VNBD `S-` files that pass it are 10 Hz streams. On top of that the yaw and pitch axes carry 2–3× the energy the white-band extrapolation predicts (per-sample σ 0.0038–0.0084 rad/s against 0.0019–0.0026 predicted), which is the desk. Loosening the gate for the device would measure the gate's tolerance rather than the sensor (D-045 point 2), so the tool keeps the threshold, prints `NO`, and requires `--include-noisy` and a directory of its own before drawing anything. (2) **Raw for the curve, compensated for the gate.** The OS gyro-bias estimate stepped 502 times in 24 min, one LSB (0.000611 rad/s) at a time; subtracting it from the curve's input would inject edges at the τ where bias instability is read, while the second difference already annihilates the constant part. The gate, which asks whether the phone is moving, must not count a static 0.006 rad/s bias as motion. (3) **The numbers are plausible for the part** — STMicroelectronics LSM6DSVTR — and sit inside the 0.5–5 °/√hr phone range the error budget states, with a VRW 3× and a bias instability 2× better than the IO-VNBD phone; they are still a desk with a PC on it, and a car seat with the engine off is the recording that would let the gate pass. (4) **Two observations from the front screen, kept because they are measurements**: the calibrated `accelerometer` stream reported 499.9 Hz for the first minutes and 220.8 Hz over the session (median Δt 2.1 ms, p95 8.1 ms) while every uncalibrated stream held 125.02 Hz — another sensor client on the phone held the calibrated accelerometer at 500 Hz and released it; the sidecar the Allan run reads was unaffected — and the harness loader again read the 10 Hz main CSV without complaint (14,663 rows, 1466.3 s). The item-7 procedure and its command are in android/HANDOVER.md §9.4. | A |

## Gate 0 — the Allan seeds regenerate from the commit their stamp names (13 Sep 2026)

| ID | Date | Decision | Reason | Owner |
|---|---|---|---|---|
| D-120 | 2026-09-13 | **The Allan seeds are regenerated at a clean commit under the tightened stop detector, and the segment finder is changed so that a sample is kept only if *every* 10 s window containing it passes; the D-045-era thresholds are *not* pinned in `eval/allan.py`.** `FilterConfig` is re-seeded from the result: `gyro_arw` 4.11e-4 → **2.18e-4** rad/s/√Hz (1.41 → **0.75 °/√hr**), `accel_vrw` 7.46e-3 → **4.03e-3** m/s²/√Hz (0.45 → **0.24 m/s/√hr**), `gyro_bias_rw` 5.48e-5 → **3.10e-5** (from B 42 → **22 °/hr**, τ_c 27.7 → 23.7 s), `accel_bias_rw` 1.04e-3 → **9.67e-4** (from B 0.34 → **0.25 mg**, τ_c 20.3 → 13.2 s); `zaru_sigma` follows to 6.894e-4 rad/s and the accel-bias block of `P₀` to 0.25 mg (`ACCEL_BIAS_INSTABILITY_MEASURED`, named so a test can hold it to the summary). Segments: `S-T2[31532:36371]` (484 s) and `S-T7[47918:52176]` (426 s). The white-band edges in `random_walk_coefficient` gain a 1e-6 relative tolerance (`BAND_EDGE_RTOL`). `allan_summary.json` gains a `detector` block recording the thresholds, window, quiet margin and segment rule, and `tests/test_allan.py::test_the_committed_artefacts_were_made_under_the_current_detector` holds it against the current `FilterConfig`. The artefacts are stamped at the code commit this row lands in (`eval/figures/allan_stamp.json` names it) and regenerate from it byte-for-byte outside the stamp line. **Supersedes D-045's numbers** wherever quoted ([ERROR_BUDGET.md](ERROR_BUDGET.md) §9.1–9.2, §10, §10.1–10.3; [METHOD.md](METHOD.md) §11.1; [DATASETS.md](DATASETS.md) §1.4; `data/README.md`; [SE23_PROPAGATION.md](SE23_PROPAGATION.md) §10; `eval/run.py`; `phases.md`); the NEES table in §10.2 is re-measured. `eval/figures/device/` (D-119) is untouched. | **The question as posed had two answers and both were wrong.** The committed artefacts (stamp `ef10dcc`, 28 Aug) stopped reproducing at HEAD when D-115 tightened `zupt_accel_var_thresh` / `zupt_gyro_norm_thresh` from 0.05 / 0.02 to 0.02 / 0.01, which `eval/allan.py` shares; D-115 noted it and did not regenerate. At HEAD the same three files give `S-T2[31433:36470]` and `S-T7[47819:52275]` — strict subsets of D-045's, 31 and 20 samples smaller — and the seeds move ARW 1.41 → 1.21 °/√hr, B 42 → 38.9 °/hr, VRW 0.45 → 0.60 m/s/√hr. Option A (follow the thresholds) makes the sensor's noise figure a function of a detector retune, and D-115 already names the next retune; option B (pin 0.05 / 0.02 in the tool) keeps the artefacts reproducible and the seeds unexplained. So the discrepancy was measured instead. *(1) The trimmed samples are motion.* On S-T2 the 31 samples carry 10× the interior gyro RMS on pitch and roll (0.0153 vs 0.0015 rad/s) and 23× on `accel_x` (0.86 vs 0.037 m/s², peak 1.53); on S-T7, `gyro_roll` 25× (0.081 vs 0.0033 rad/s, peak 0.125 rad/s = 7 °/s). That is a car settling on its suspension and pulling away — and the D-115 bounds still hold it: S-T7's last second reads 74× the interior gyro RMS. The reason is the finder's geometry, not its thresholds: it kept the *union* of the windows that pass, and a 10 s mean lets up to a second of vehicle motion through at each end, more the looser the threshold. *(2) The seeds were that second.* Trimming the D-115 bounds by 0 / 2 / 5 / 9.9 / 15 s at each end gives ARW 1.22 / 1.17 / 1.14 / **0.75** / 0.75 °/√hr, VRW 0.60 / 0.48 / 0.45 / **0.24** / 0.24 m/s/√hr, gyro B 38.9 / 32.9 / 25.4 / **22.0** / 22.1 °/hr — a plateau at ~10 s, which is one window: exactly the samples *every* covering window passes on. ARW and VRW are read at τ = 0.2–2 s, so 0.6% of the samples set them. Every D-045 seed was about 2× cabin transient, and the tool's own comment on `SLOPE_TOLERANCE` — a "low-frequency bump" on S-T2's `accel_x` — was describing that transient. *(3) Why the intersection rule rather than a pinned threshold.* The finder's stated intent is that Q is characterised on the samples the filter will ZUPT on; the runtime detector fires on the last sample of a passing trailing window, so a sample the filter ZUPTs on is one every covering window passes on. The intersection is the more faithful reading, and it makes the seeds insensitive to the threshold to first order: the bias instabilities agree between the D-045 and D-115 thresholds to 0.3%. A run at the record's own edge is not eroded there (nothing failed before it), which is what keeps D-119's first desk run intact. *(4) A second defect, also in D-045's run.* Every τ is `m · dt_s` and `dt_s` is a median of float differences: S-T2's is 0.09999999999990905, S-T7's 0.1000000000003638, so an exact `τ ≥ 0.2` kept τ = 2 dt on one file and dropped it on the other. That one point moved S-T2's `gyro_pitch` slope from −0.32 to −0.36, across the ±0.15 whiteness gate, and the seed ARW by 22% (0.92 against 0.75). Fixed with a 1e-6 relative slack on the band edges; `test_the_white_band_does_not_depend_on_the_last_digit_of_dt` pins it. *(5) What the gate says now, stated as a range.* Five of six accelerometer fits are white (D-045: one of six) and three of six gyro fits are not — S-T2 `gyro_pitch` and S-T7 `gyro_yaw` / `gyro_pitch` sit at −0.26 to −0.32 over τ = 0.2–2 s, flatter than a random walk; the 1–2 s of residual settle the looser thresholds admit is what made them *look* white (S-T2 `gyro_pitch` reads −0.51 with it), so under the D-045 thresholds and the intersection rule ARW would be 1.00. The seed sits on a knife edge of the gate; the honest statement is the range, 0.41–0.75 °/√hr and 0.18–0.24 m/s/√hr, with the worst white axis seeding Q as D-045 chose. *(6) Effect on the filter.* In motion, `in_motion_config` floors at the Allan value and D-115 measured the stream 20–200× above it, so Gate 1 in motion is untouched. At a standstill the ZARU `R` halves in σ, and the accel-bias `P₀` block tightens 1.36× — the wrong direction for D-115's open item (the estimate walks 15–25σ on S1), which this makes more pressing, not less. NEES re-measured under the new `Q` and `P₀`: 17.42 / 18.59 / 16.62 / 18.67 / 13.57 against 18.29 / 18.93 / 17.03 / 19.03 / 14.14, every verdict unchanged. *(7) D-119.* The Galaxy A55 sidecar under the new rule (scratch, not committed): 534.6 / 347.2 / 185.2 s, none quiet, ARW 1.19 → 1.20 °/√hr, B 18.6 → 19.0 °/hr — a desk has no settle to trim. Its artefacts reproduce from their stamp (`d621cc0`) and stay as they are. *(8) The gap that let this sit for sixteen days.* `test_filter_config_matches_the_measured_summary` compared `FilterConfig` with the committed summary, and both were old together; the summary now records the rule its segments were found under, and the new test compares that with the *current* `FilterConfig`, so the next retune fails with a message naming the regeneration it needs. Diagnostic scripts in the 13 Sep session scratchpad (`edges.py`, `erode.py`, `thresh.py`, `nees_blocks.py`). | S (filter), D (harness) |
| D-121 | 2026-09-13 | **The operator UI (`android-ui/`) builds its navigation screen on the Mapbox Navigation SDK for Android, core SDK v3 (`com.mapbox.navigationcore` 3.30.1 + `ui-maps`) with the Maps SDK Compose extension (11.30.1) -- not the UX Framework "Dash" -- on the classic `NAVIGATION_DAY/NIGHT` styles for v1. The module gains two product flavours, `osm` (the D-117 OSMDroid canvas, default, needs no account) and `mapbox`; everything outside `ui/components/MapView.kt` and `MapStack.kt` is shared.** | The user chose the Mapbox UI on 13 Sep; the review of that SDK against this project's rules is `docs/UI_UX_NAVIGATION_PLAN.md` section 7, which is committed with this row. Core v3 over Dash for the three reasons in section 7.1: every Stage 2-5 element of the plan is bespoke chrome plus FSM-driven camera and theme changes, which the core SDK exposes as live options and Dash as init-time config; Dash's value (search, route preview, arrival, EV, MapGPT) is outside PS 26168 and its cost (beta API churn, evaluation terms, snapshot Maven, an `Application` subclass and Fragment host) is the class of dependency D-111 warns about; and `NavigationScreen.kt` already is the assembled screen, so swapping the map composable is the smaller change. Classic styles over the Standard light preset because route line and arrow have no slot choice on Standard yet and the buildings API is not Standard-compatible (section 7.6). The flavour split exists so that a machine or CI runner with no Mapbox account still builds and tests the module (D-114 stays true) and so that the two engines can be compared on the same phone. A two-day Dash spike remains an option, logged as its own row if taken; both are not run. | A |
| D-122 | 2026-09-13 | **`android-ui/` re-admits the Mapbox stack, a network dependency far larger than D-117's OSM tiles, under a rehearsal rule: the venue build of the `mapbox` flavour must survive airplane mode over a pre-downloaded `TileStore` region (Maps + navigation tiles, one `TileStore` instance shared by `MapboxMapsOptions.tileStore` and `RoutingTilesOptions`), and the finale rehearsal is run with the SIM out and Wi-Fi off with the `getAllTileRegions` listing screenshotted into the run log. `android/` (the logger) is unchanged: no Mapbox, no `INTERNET`. Tokens never enter the repository: the secret `Downloads:Read` token lives in the per-machine `~/.gradle/gradle.properties` (CI: the `MAPBOX_DOWNLOADS_TOKEN` secret, passed as `ORG_GRADLE_PROJECT_MAPBOX_DOWNLOADS_TOKEN`), the public token in the gitignored `android-ui/local.properties` and is injected as a `resValue`. `settings.gradle.kts` declares the Mapbox Maven repository only when the secret is present; CI builds `osm` always and `mapbox` only when the secret exists; `tests/test_android_demo_surface.py` drops `maps-compose` from its forbidden list, adds `play-services-location` to it, and fails on any `pk.`/`sk.` literal under the module.** Amends D-111, extends D-117. | Mapbox is online-first: tiles, the Directions API, the Voice API and road-object data all arrive over the network, and the on-device router works only over a cached region (section 7.4). D-111's reason stands -- a demo that works on the rehearsal phone and fails at the venue is worse than a blank map -- so the network is admitted with the rule that makes the venue build independent of it, not silently. Predictive caching is not a substitute (it caches around a drive that already happened online). Custom routers were removed in SDK v3, which retires the tunnel doc's Valhalla/GraphHopper plan on this stack. The token rule is D-111's `com.google.android.geo.API_KEY` lesson applied before the mistake rather than after: a token that works in rehearsal and is revoked before the venue is worse than none. Gating the repository on the token is what keeps `assembleOsmDebug` green on a fork. **Open (section 7.11 row 5):** who owns the Mapbox account, and the MAU budget -- every test phone is a billed monthly active user and a Maps MAU is billed on top when a map is shown. | A |
| D-123 | 2026-09-13 | **Puck ownership. The InEKF is the only estimator that moves anything on the Mapbox map, and the screen makes that provable. (R1) The filter pose is fed to the SDK through a custom `DeviceLocationProvider` (`backend/mapbox/InekfLocationProvider.kt`, type `REAL`) at a fixed 10 Hz whether or not the pose changed -- inside a tunnel, at a stop, always -- with `navigatorPredictionMillis` matched to that cadence. (R2) `NavigationOptions.enableSensors(false)`, explicitly, with a comment naming section 7.2; a text test fails if it is ever `true`. (R3) Every `Location` field is filled from the state: `horizontalAccuracy` = 1 sigma along the covariance major axis, `bearing` = filter yaw, `speed` = filter speed, timestamps = the pose's own session clock. (R4) A second, IDR-owned layer -- track, 1 sigma ellipse from the 2x2 P block, raw pose dot -- is drawn by us from `TelemetryState` alone, separate from the SDK's location component; when the SDK puck arrives with the trip session both stay on screen and labelled. Deferred to the replay/live milestones: (R5) log `onNewRawLocation` and `onNewLocationMatcherResult` to a sidecar; (R6) `DeviceProfile` AUTOMOBILE vs HANDHELD as an experiment; (R7) reroute paused in `TUNNEL_ACTIVE_IDR` / `EXIT_VERIFICATION`. `play-services-location` is forbidden by test.** | The SDK ships its own dead reckoning and extrapolates across any gap in its feed; with `enableSensors(true)` it "ignores location updates which don't match data from sensors" (its own words, `NavigationOptions.Builder`). If nothing were done, a tunnel demo would show Mapbox's extrapolation and not ours, and a judge who knows the SDK would ask a question nobody could answer from the screen. R1 removes the gap the extrapolator needs; R2 removes the cross-check that would drop our feed; R3 is what makes the map matcher weight our updates sanely and `zLevel` / off-route behave at a tunnel exit; R4 is the only visual that proves which estimator is driving, and it has to be an ellipse because the filter's uncertainty is lateral-dominant (AGENTS.md: lateral error is about half of b_g v t squared) while the SDK's accuracy ring is a circle. Google's fused provider would be a second source next to ours, which is R1 broken by a dependency line. | A |
| D-124 | 2026-09-13 | **On-device display vocabulary, for every Android surface including the new Mapbox screens: position uncertainty is shown as `est. σ` (from the covariance) and a tunnel exit as `exit residual vs GNSS` (‖p_IDR − p_GNSS,first accepted‖, captioned as a residual against a fix, not against truth) with the χ² accepted/rejected counts. No Android surface prints "drift", a drift percentage, or a "grade". `tests/test_android_demo_surface.py` fails on a string literal that uses drift as a label or awards a grade; the D-081 caption may still say what a figure is *not*.** Restates D-112 for the plan's Stage 3 HUD, Stage 5 toast and bottom sheet, all of which carried a drift figure and one a "Grade: A". | Drift is error against truth as a percentage of distance -- the graded metric -- and a phone has no truth, so any on-device drift figure is a number never measured against truth, rendered in the same typeface as one that was (D-112's reason). "Grade: A" is a self-awarded score on an unmeasurable quantity. The exit residual is the one thing that *is* measured at a tunnel mouth. Enforced by text test because the plan documents were written with the drift cards in them and the pattern would otherwise be copied straight into Compose. | A |
| D-125 | 2026-09-13 | **`TelemetryState` (the `DeadReckoningBackend` contract, `android/HANDOVER.md` section 4 item 12) grows the 2x2 position covariance in the filter frame -- `covNorthM2`, `covNorthEastM2`, `covEastM2` -- and the pose's session clock `poseElapsedMs`; `uncertaintyM` stays as the 1 sigma major-axis figure the metric card prints, kept consistent by the producer. `backend/Covariance.kt` is the one closed-form eigen-decomposition between the covariance and the drawn ellipse (orientation normalised to (-pi/2, pi/2]), used by both flavours and unit tested (`app/src/test`, the module's first tests; CI runs them). `LocalNavigationEstimator` fills sigma squared on the diagonal and zero off it, with a comment saying that is a faithful statement of a scalar and not a measured ellipse.** | A scalar radius cannot carry the InEKF's uncertainty, which is an ellipse whose long axis is lateral and yaw-driven; a map matcher handed a radius (R3) and a map drawing a circle (R4) would both be lying about the filter's shape. The seam is defined now, before the JNI layer exists, so that the filter fills three numbers into a contract the screens already draw rather than the screens being rewritten when it lands. The demo estimator's isotropic fill is the honest representation of what it knows, and the comment is there so nobody reads the circle as the filter's. `HANDOVER.md` item 12 gains the `latPerMetre` flat-earth note from section 7.2: fine for a demo, not what should feed a map matcher. | A |
| D-126 | 2026-09-13 | **The tunnel state machine of `docs/TUNNEL_MODE_AND_NAVIGATION_UPGRADES.md` section 1.2 is implemented as pure Kotlin with an injected clock (`android-ui/.../backend/tunnel/TunnelFsm.kt`, 33 JUnit scenarios), and it -- not a toggle -- decides when the operator UI's estimator suppresses GNSS. States and thresholds are the document's (C/N0 healthy > 28 / suspect < 24 / lost < 18 dB-Hz, portal 40 m pre-arm and 100 m hazard, 1.2 s silence, lux < −5000 lux/s, baro +0.3 hPa over 200 ms, 2 consecutive χ² passes, ~2 s blend), held in `TunnelFsmConfig` because none has been measured on the team phone. Inputs wired today: `GnssStatus` (used-in-fix count, mean C/N0 of the best four), `Sensor.TYPE_LIGHT`, `Sensor.TYPE_PRESSURE`, every fix's gate verdict; `inTunnel` and portal distance are inputs fed by nothing until the `mapbox` flavour runs a trip session. Five deviations from the diagram, each pinned by a test: (1) a reverse edge `PRE_ARMED_ENTRY → GNSS_HEALTHY` after 3 s of healthy signals; (2) a rejected fix in `EXIT_VERIFICATION` resets the pass counter and the state falls back only on renewed silence; (3) the estimator's anti-lockout acceptance after 5 consecutive rejections (D-115's rule) is recorded as `CHI2_FORCED`, never as a pass; (4) the timeout out of `GNSS_HEALTHY` needs C/N0 unhealthy, unknown or older than 3 s; (5) map inputs are edge-triggered against the last exit, and a C/N0 recovery counts only if reported after entry. HDOP is not a trigger (`GnssStatus` does not expose it) and no sub-second latency is claimed. The χ² gate (`mahalanobisSquared`, threshold 9.21) runs in `LocalNavigationEstimator` only while GNSS is suppressed; the manual pill becomes a force override whose release runs the exit path; every transition is logged with its trigger; the Stage 5 toast shows only measured fields (`TunnelExitSummary`: metres on IDR, elapsed, exit residual vs the first accepted fix, gate counts, forced or not) per D-124.** | The document's Phase 1 objective is to eliminate the manual toggle, and section 7.11 kept the FSM with one more entry trigger and two exit-side overrides. Pure Kotlin with time injected is the only form in which every transition of a demo can be reproduced and argued about afterwards (D-080), and the only form a laptop can test -- 33 scenarios run in CI on every push. The deviations are what the document's own goals require once written down: it asks for no false positives under a footbridge, which a pre-arm with no way back cannot deliver; its loop-back edge on a rejected fix would flash the status pill at 1 Hz at every portal; a gate with no anti-lockout leaves a drifted pose unable to re-acquire, and hiding the forced acceptance as a pass would put an unverified claim on screen; a 1 Hz receiver jitters past 1.2 s on ordinary days; and `inTunnel` stays true for the whole bore. The gate is applied only in the dead-reckoning states because this estimator's pose is anchored to the last fix everywhere else, so at highway speed a 1 Hz innovation is tens of metres by construction and would be rejected -- the InEKF, which propagates, will gate everywhere. Thresholds stay in a config object rather than constants because the D-116 rule -- record first, then set -- has not been run for C/N0, lux or pressure on the A55; the callback rate of `GnssStatus` in particular is unmeasured and is what bounds the entry latency the document quotes. Not verified on a device this session (the phone was not reachable); the desk path (no fix → `GNSS_TIMEOUT` → IDR, force on/off, toast) is the first thing to run. | A |
| D-127 | 2026-09-14 | **The operator UI's demo estimator stops taking its heading from the handset's attitude and its speed from a 60 ms silence. Heading moves into `backend/CourseTracker.kt` (pure Kotlin, injected clock, 17 JUnit scenarios): the yaw rate is the gyro's projection onto the world vertical `-(omega . u)` with `u` the rotation matrix's third row -- not the device-frame z rate -- the absolute reference is the GNSS course over ground above 2 m/s at gain 0.25, and the handset's own azimuth carries only a mount offset, read off whichever device axis is furthest from vertical (sticky, and a switch re-derives the offset rather than turning the course). Three freezes: below 0.7 m/s nothing integrates; a tilt rate over 25 deg/s or a yaw rate over 200 deg/s holds the course for 0.8 s and re-learns the offset against the unchanged course on the falling edge; what survives is clamped to 60 deg/s. Motion detection moves into `backend/MotionClassifier.kt` (13 scenarios): the standstill hold is a duration (0.6 s on foot, 2 s in a vehicle) and not 12 samples, a receiver speed at or above 1 m/s vetoes it for 3 s, the gravity-magnitude tracker runs at a 20 s time constant and is seeded from the first sample only if that sample is within 1 m/s^2 of standard gravity, and the pedestrian step model is off entirely once a fix has reported 4.5 m/s. `onLocation` takes `location.speed` whenever the fix carries one, including below 0.3 m/s; the tunnel coast holds constant velocity with a 120 s bleed instead of a 0.35 m/s^2 ramp. `TelemetryState` gains `headingIsCourse`, `mountOffsetRad`, `attitudeDisturbed` and `motionMode`, and the diagnostics drawer shows them as one chip.** | Field test, 14 Sep: the traced curve was wrong and its direction followed the phone. Both halves were measurable in the source. **Heading.** `onOrientation` was handed `atan2(R[1], R[4])` -- the azimuth of device +y projected onto the ground plane -- and assigned it to `yawRad`, so any fixed mounting angle rotated the whole track by that angle, and a phone upright in a cradle (+y at the sky) was computing `atan2(0, 0)`, which is where 'a couple of degrees of tilt swings the heading' comes from. The gyro path had the same premise and two defects: device z is the world vertical only when the phone is flat, so a cradled phone integrated a fraction of each turn, and the sign was inverted -- a right-hand-positive rate about up is counter-clockwise from above and *decreases* a compass bearing, and the code added it. The map rotates with `yawRad` in course-up mode, so on screen the whole canvas turned with the handset. Note what the fix does **not** claim: a pure twist about the vertical with no tilt change and no implausible rate is indistinguishable from a vehicle turn on IMU alone, which is why the stationary freeze carries as much of this as the tilt detector does. **Speed.** `STATIONARY_FRAME_COUNT = 12` at the requested 200 Hz is 60 ms of quiet, which smooth tarmac delivers several times a second; each time, `speedMps` was set to zero, which is the flickering speedometer and also a track that stopped advancing, since the dead-reckoning branch propagates along `speedMps`. The gravity baseline's `0.985/0.015` at 200 Hz is a 0.33 s time constant, so sustained acceleration was absorbed into 'gravity' and read back as zero dynamic accel. And nothing gated the pedestrian step model by mode: road and engine noise past 1.2 m/s^2 queued 0.5-0.8 m strides at up to 3.8 Hz -- roughly 2.5 m/s of invented travel, laid down along `stepHeadingRad` frozen at the last 'step' -- which is the second half of the wrong curve. The mount offset is the same quantity as `R_sv` (D-006), estimated here from the GNSS course rather than by PCA because this demo has a course and the reference filter's streams may not; it is an offset the filter carries, never a heading, for D-006's reason. The chip exists because both bugs were silent: a heading that was the handset's azimuth rendered identically to one that was the vehicle's course. Not verified on a device this session; the phone is the next step, and the chip is what that test reads. | A |
| D-128 | 2026-09-14 | **The operator UI's dead reckoning gains a gyro null-offset estimate, applies the anti-lockout fix it forces, and slews a re-acquisition instead of teleporting it.** (1) `CourseTracker` integrates `rate - bias`, never the raw rate. The bias is a scalar about the world vertical, measured in the two places it is observable: at a standstill the classifier has already declared (ZARU, 4 s time constant, not while the phone is being handled), and over a window of at least 4 s of GNSS-tracked driving, where the total the anchor had to put back divided by the window is the rate the gyro got wrong, taken at gain 0.25 and clamped to 5 deg/s. No window may span an outage (`onGnssDenied()`, called for every dead-reckoned sample and at both edges), so what runs inside a bore is what the last clean stretch of driving measured -- AGENTS.md's "bias snapshot at tunnel entry", reached by never opening a window that could overwrite it. (2) `LocalNavigationEstimator.onLocation` applies a `FixVerdict.FORCED` fix instead of counting it and returning; the machine still records `CHI2_FORCED` and never a pass (D-126 deviation 3 is unchanged). (3) A re-acquisition -- the forced fix, or the first fix after the machine stops suppressing -- moves the *state* onto the fix whole and moves the *reported* pose not at all: `backend/ReacquisitionSlew.kt` (pure Kotlin, 8 scenarios) takes the same step backwards and decays it with a 0.7 s time constant, so ~94% is behind the track inside `TunnelFsmConfig.reconvergeSettleMs`. The offset is capped at 75 m, is added to the reported sigma while it runs so the ellipse still covers the estimate, is never counted as distance travelled, and never feeds back into the estimate; the fixes after the re-acquisition are blended as before. `TelemetryState` gains `gyroBiasRadPerSec` and the D-127 chip gains a `bias +1.0°/s` field. 18 new JUnit scenarios (10 on the bias, 8 on the slew), 2 on the label. | **Field test, 14 Sep, Wazirabad approach: the speedometer of D-127 was fixed and the traced curve was still wrong, and the screenshot says how.** The banner reads 235 m on IDR over 20 s with 16 accepted and 20 rejected fixes, re-acquired "by the anti-lockout rule, not a pass" -- and the track leaves the bore, runs out to a vertex in open ground and comes straight back. Three separate defects, one per clause above. **(1) Nothing estimated the gyro bias.** `CourseTracker` integrated the projected rate raw, and the only thing that had ever hidden that is the GNSS anchor, which is exactly what a tunnel removes. AGENTS.md's first risk row names the remedy ("ZARU at every detected stop, bias snapshot at tunnel entry") and `TunnelState.PRE_ARMED_ENTRY` has said "the filter snapshots its gyro bias" since D-126; neither existed in this module. The cost is AGENTS.md's lateral term, and it is not small: simulated over this outage's own numbers -- 20 s, 14 m/s, 280 m -- an uncompensated 1 deg/s puts 20.0 deg of heading and **48.4 m** of lateral error into the bore (0.5 deg/s: 24.4 m; 2 deg/s: 93.9 m), against **0.33 m** after one traffic-light ZARU. Learned from GNSS-tracked driving alone, with no stop, two minutes converges to 1.01 +/- 0.05 deg/s under 3 deg 1-sigma bearing noise and 1.01 +/- 0.14 deg/s under 8 deg (20 seeds each; the scripts are in the 14 Sep scratchpad). **(2) The anti-lockout rule was a label.** D-115's rule exists because a pose that has drifted past the gate's width rejects every fix it is offered; the gate branch computed `FORCED`, incremented the counters, told the machine, printed "applied by the anti-lockout rule" -- and returned before touching the pose, under a comment saying "evaluated, counted, not applied" that was written for the rejected case and swallowed the forced one. So the 20 rejections on screen never re-acquired anything and the drift ran until the machine left the suppressing states on its own. That is also an unverified claim rendered in the same typeface as a measured one, which is what D-124 exists to prevent. **(3) The exit was a teleport.** The first fix to apply after an outage carries the whole accumulated drift, went in at `alpha` up to 0.85 in a single step, and was written straight into the polyline by `appendTrackPoint(force = true)` -- the spike, drawn. AGENTS.md's thesis is that one filter means no position jump at a tunnel mouth; the demo estimator does have a jump, so the promise is kept where it can be, on the display, and the state still takes the better answer at once. Slewing the state instead would put the error back into the estimate, so it is an output offset and the sigma carries it. Why a scalar bias and not the gyro's three-axis offset: a scalar is what a scalar course can use, re-attituding the phone invalidates it and the next ZARU re-learns it, and the InEKF carries the vector properly -- saying so is cheaper than implying a calibration this does not do. Not verified on a device this session: the container has no Android SDK and `dl.google.com` is unreachable from it, so `:app:testOsmDebugUnitTest` could not run here and CI is what runs it. The pure-Kotlin units were compiled and their 35 scenarios run standalone against `kotlin-compiler` 2.0.21 and JUnit 4.13.2 from Maven Central, and the Android-coupled `LocalNavigationEstimator` and `Telemetry` were type-checked against stubs for `Location` and `SystemClock`. The `bias` field on the chip is what the next road test should read at the portal. | A |
| D-129 | 2026-09-14 | **`fix/calibration-and-audit-gaps` (607a293) lands with two of its four clauses amended in the follow-up on `main`.** Kept as written: the HAL drift seed (`CourseTracker.seedHardwareDrift`, fed from `TYPE_GYROSCOPE_UNCALIBRATED` `values[3..5]`, taken once, before this class's own ZARU or GNSS-window measurement exists) and the R-8 / D-059 audit close-out in METHOD.md §13 and SUBMISSION_AUDIT.md, which D-085 already established with the numbers the text quotes. Reverted: the yaw-rate gate on `observeBias` (`MAX_BIAS_OBSERVE_YAW_RATE = 0.08` rad/s, corrections skipped while the last rate exceeded it) -- `observeBias` is back to D-128's form, with the reason in a comment. Rewritten: the bearing fallback in `LocalNavigationEstimator.onLocation` for a fix without `hasBearing()` now takes the direction from the previous *applied* fix to this one (`lastAppliedFix`, gap ≤ 2.5 s, step ≥ 3 m, speed ≥ `COURSE_ANCHOR_MIN_SPEED`) instead of `atan2(dEast, dNorth)`. `seedHardwareBias(Float)`, which nothing called, is removed. METHOD.md's D-075 paragraph names the constant correctly (0.44, not 0.45) and says what the tilt rule sees: a tilt knock, not a yaw knock. | **The gate made the thing it targeted worse, and the number is not close.** The lag it names is real -- a receiver's course over ground trails the true heading through a turn -- but the anchor's response to it has two halves: while the turn is on, the stale bearing pulls the course *back*; once the bearing catches up, it pushes the course *forward* by the same amount. In D-128's window both halves are summed and cancel. The gate drops only the first half (the rate is high while turning, low again by the time the bearing catches up), so the catch-up is read as a rate error the gyro never had. Simulated with the same loop as D-128's figure -- 100 Hz gyro, 1 Hz anchor at gain 0.25, 4 s window, learn gain 0.25 -- over 150 s with five city turns of 8–12 deg/s and the bearing 0.8 s late, 20 seeds under 3° bearing noise: **learned bias for a perfect gyro −0.04 ± 0.05 deg/s without the gate, −0.11 ± 0.07 with it; for a 1 deg/s offset 0.96 vs 0.89.** At 1.5 s of lag: −0.08 vs −0.22. Straight driving and a 40 s sweep at 5 deg/s are identical under both, which is why no existing test caught it: `a_real_turn_is_not_read_as_an_offset` drives a perfect, unlagged bearing. The new `a_lagging_bearing_through_turns_is_not_read_as_an_offset` drives the lagged one, noise-free, and asserts under 0.1 deg/s: it reads −0.137 deg/s with the gate and −0.054 without, so it fails on 607a293 and passes on this. Closing the window during turns instead (a third variant) lands at −0.09, and adding a 2 s settle after each turn brings it back to −0.04 -- level with, not better than, no gate at all, so nothing replaces it. **The bearing fallback's input was the wrong vector.** `(dNorth, dEast)` is fix minus *estimate*, and between fixes with GNSS fresh the estimate is not propagated, so on an ordinary drive it happens to point roughly along the travel; but the case the fallback exists for is the fix that arrives without a bearing after a dead-reckoned stretch -- the forced fix at a portal, the first after a flyover -- and there the innovation is the accumulated pose error, which points wherever the drift went. With `deltaFixM ≥ 1 m` as the only guard, that is exactly the fix it would have anchored to, at gain 0.25. Two consecutive applied fixes are a direction of travel; fixes the gate evaluated and did not apply are not recorded as `lastAppliedFix`, so after an outage the gap check (2.5 s) refuses the first bearing rather than draw one between two multipath positions. `Location.bearingTo` is what `onLocation` already uses for the origin-relative frame. Verified: `:app:testOsmDebugUnitTest`, 154 tests, on the Windows box. One toolchain note: under Gradle 9.6 the daemon JVM's launch arguments did not carry the `-Djdk.net.unixdomain.tmpdir` that `~/.gradle/gradle.properties` puts in `org.gradle.jvmargs` (its log shows only `-Xmx2048m -Dfile.encoding=UTF-8 -Duser.*`), and every build died at the AF_UNIX loopback the android README documents; exporting `JAVA_TOOL_OPTIONS=-Djdk.net.unixdomain.tmpdir=C:\Users\MANISH~1\.gradle\afunix` -- which the JVM reads itself, before Gradle sees anything -- is what got the daemon up. | A |
